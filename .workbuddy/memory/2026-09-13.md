# 2026-09-13

## 四项功能改进（12:36–13:10，提交 185c9b1）
- **① 内容渲染增强**：KaTeX 0.16.11 + Mermaid 9.4.3 **自托管**进 `app/static/vendor/`
  （CSP 是 script-src 'self'+nonce，不能引 CDN；KaTeX CSS 打了补丁只留 woff2 字体）。
  `markdown_render.py`：```mermaid 围栏先抽成 `{{mm:N}}` 占位符（在 escape_raw_html 之前），
  渲染后还原成 `<div class="mermaid">转义后代码</div>`；`Rendered` 新增 has_math/has_mermaid，
  模板经 `_enrich.html` partial 按需加载；`static/js/enrich.js` 统一渲染。
  公式只认 `$$…$$` / `\[…\]` / `\(…\)`，**单 $ 不触发**（价格误伤）。
  **坑：mermaid v9 `init(undefined, [el])` 数组会静默不渲染，必须传单元素或 NodeList**。
- **② 多文件导入**：`/backup/import` 的 `file` 参数改成 `list[UploadFile] = File(..., alias="file")`，
  表单加 multiple，计数合并；导入服务层（importer.py）本来就完整，无需动。
- **③ 键盘导航**：app.js 新增 initKbdNav（j/k 或 ↑↓ 选中 .note-card、Enter 走标题链接原生行为、
  x 勾选批量框、? 呼出 base.html 的 `<dialog id="kbd-help">` 速查浮层）。
- **④ /docs 收口**：`settings.docs_enabled = _env_bool("INKNOTE_DOCS", False)`，create_app 里
  docs_url/redoc_url/openapi_url 按开关置 None。
- **验收**：665 passed；audit_css OK；check.py 全过；浏览器端到端验证（真 Chrome）：
  KaTeX 排版、Mermaid SVG、j/k/?/Esc 全部实测通过。
- 踩坑：playwright 测试忘了在浏览器里登录，页面被踢到登录页导致"没渲染"的假象。

## 手机可用性实测（13:14）
- 390×844 视口（playwright is_mobile）扫 10 个页面：发现内容页横向溢出 819px
  （不带空格的超长单词/URL 不换行，.prose 与卡片缺 overflow-wrap）。
- 修复：`.prose, .note-card, .post-card, .card__summary, .post-summary, .page-sub`
  加 `overflow-wrap: anywhere`，复测 10 页全部 0 溢出。已提交。
- 结论：手机浏览器直接用没问题（viewport meta、640px 断点、导航开关都有）；
  没做 PWA（无 manifest/service worker，不能装成 App/离线）。
  手机要访问需 `INKNOTE_HOST=0.0.0.0`（默认 127.0.0.1 只监听本机）。

## Cloudflare Tunnel 部署（13:25–13:45）
- 用户放弃局域网直连（防火墙要管理员提权），改走 Cloudflare Tunnel。
- cloudflared 经 winget 装好（`C:/Program Files (x86)\cloudflared\cloudflared.exe`，
  winget 静默安装无输出，别以为失败了；GitHub 直连下载会超时，镜像返回的是错误页 HTML）。
- **启动入口是 `run.py`，`python -m app.main` 会静默退出**（app/main.py 没有 __main__ 块）。
- 快速隧道：`cloudflared tunnel --url http://127.0.0.1:8000`，免账号，URL 每次随机
  （本次 echo-jews-balance-mechanical.trycloudflare.com），后台任务挂着。
- 密码优先级：**数据库 meta（设置页改过密码）> INKNOTE_PASSWORD_HASH > INKNOTE_PASSWORD**；
  已实测默认密码 401 被拒，用户的自有密码生效。
- .env 已改回 INKNOTE_HOST=127.0.0.1（隧道只需本机访问）。
- 风险提示：*.trycloudflare.com 在中国大陆访问不稳定，手机可能需要代理；
  要固定域名需注册 Cloudflare 账号建 named tunnel。

## 部署结论（13:49）
- 用户权衡后放弃上云：Workers 重写太麻烦（数据层 sqlite3→D1 全重写 + 第三方包 beta 限制），
  VPS 又要花钱。**维持现状：本地运行 + Cloudflare 快速隧道**（免费，电脑开着时手机可访问）。
- cloudflared 已装好在 C:/Program Files (x86)\cloudflared\，随时可再开隧道。

## AI 免费预设（14:04 提交 baee477）
- PROVIDERS 加硅基流动（https://api.siliconflow.cn/v1，bge-m3 向量无限免费）和
  智谱 GLM（https://open.bigmodel.cn/api/paas/v4，glm-4.7-flash 对话免费不限量，注意 base 以 /v4 结尾不是 /v1）。
- 防臃肿设计：featured 三张卡平铺带「免费」徽章，DeepSeek/OpenAI/通义收进
  `<details class="ai-presets-more">`；卡片渲染抽成 preset_card 宏。
- 用户 13:52 杀掉了我挂的后台服务/隧道（他们要自己管）；给了 start-local.bat 与 start-inknote.bat。

## 设置页重排（14:11 提交）
- 问题：站点信息默认展开占满首屏、AI 相关块被安全与隐私打断、12 个折叠块无分组、
  底部说明性内容混排。
- 重排：AI 服务唯一默认展开 → AI 相关块（分任务/安全/向量/提示词/用量/常见服务商）
  → 「站点与账号」组（站点信息收起）→ 「参考」组（.env 选项/配好 AI 能干什么）。
  settings-group 分组标题、chevron 随展开旋转、settings-form__actions 加分隔线。
- 用 playwright 截图前后对比驱动（.scratch/settings_shot.py，改端口可复用）。

## 获取模型列表完善（14:32 提交）
- 四个短板：缓存不分服务商（换地址显示旧列表）、不区分对话/向量模型、不标免费、后端只回名字数组。
- 修复：FREE_MODELS 快照 + free_models_for() → /api/ai/models 响应带 free；
  前端缓存存 {base, models, free}，change/预设点击触发 reloadModelsForBase()；
  combo 渲染免费置顶 + .combo__free 徽章；向量下拉按 embed|bge 正则过滤。
- 注意 FREE_MODELS 是手工快照（以服务商价格页为准），智谱 base 是 /v4 结尾。

## Agent 化三步（14:48–15:10 提交 1fdfb29，开了两个子代理并行）
- **① 笔记助手**（services/agent.py + /agent 页 + agent.js + /api/agent/run）：
  JSON 协议工具循环（不走原生 function calling，任何 OpenAI 兼容端点可用），
  8 个白名单工具全走 repo，max 6 步，观察截断 800 字；模型不按格式回就当最终回答。
- **② 夜间整理**（services/tidy.py）：每小时检查、24h 幂等（meta tidy.last_run）；
  未分类 ≤20 篇/天 AI 补分类、收件箱→已归档 ≤50 篇；INKNOTE_TIDY=0 关闭。
  子代理修了 naive/aware datetime 混用 bug。
- **③ 问笔记多跳**（ai_chat.reflect_and_expand）：非流式 JSON 判断（enough/search），
  ≤2 追加跳、按 id 去重合并来源；解析失败当 enough；ask_submit 保持 0 跳防回归。
- **顺手修真 bug：AI 请求走系统代理**。沙箱/系统代理会拦 127.0.0.1 返回 502
  「upstream connect failed」，此前只在本机直连时暴露。新增 ai._urlopen：
  is_local_address 的目标用 ProxyHandler({}) 绕过代理，ai.py/ask.py/ai_embed.py 统一。
  **Ollama + 系统代理用户会踩这个坑**——以后任何新 AI HTTP 调用都必须走 _urlopen。
- 测试 665 → 696。git index.lock 竞态又出现两次（并发/中断后），rm 掉即可。
- 子代理并行经验：两任务文件不重叠时很有效；汇报要抽查（发现 1 个测试失败并修复）。

## 三项收尾（15:42–15:55，多子代理并行）
- 执行进度 SSE（iter_agent_events 生成器 + /api/agent/stream + getReader 前端）
- 夜间整理可见（tidy.describe + settings.html 夜间整理块）
- 只读模式（agent 页 checkbox，_WRITE_TOOLS 拦截，API/流式均透传 read_only）
- agent 循环 run_in_threadpool 防阻塞事件循环
- 教训：改子代理写过的文件前必须先 grep 实际签名（A 重构过 iter_agent_events）；
  JS 字符串替换插错位置会出语法错误，替换后必须 node --check。
- 测试 665 → 706（今天 +90）。

## Agent 提升（16:06–16:15）
- 会话记忆：客户端 localStorage 存最近 5 轮，请求带 history，服务端 _clean_history 收敛；
  助手页有「本轮会话」区 + 清空按钮。
- read_note 4000 字 + offset 分页；系统提示词加行动准则（先查再写/含糊就反问/写后复查）。
- 坑 1：SYSTEM_PROMPT 里 JSON 示例的花括号必须 {{}} 转义（str.format 会吃掉）。
- 坑 2：run_agent 转调 iter_agent_events 时新参数容易漏传（history 就漏了，测试抓到）。
- 测试 696 → 709。

## Agent 审计与韧性（16:35–16:50）
- 执行历史落 meta agent.runs（cap 20），list_runs/clear_runs + 两个端点 + 助手页「历史任务」区；
  模型调用重试（CHAT_RETRIES=2）；步/final 事件带 involved 笔记（前端渲染成链接）。
- 坑：观察结果的笔记键有 note_id/id/notes[] 三种形状，收集要兼容；
  并发测试共享 session DB，断言计数前先 clear_runs。
- 移动端 390px 实测助手页 0 溢出。测试 709 → 713。

## 模板功能增强（16:57–16:35 提交 d0f5e6e）
- services/note_templates.py：render_variables（6 种变量，宽容空格变体，未知占位符保留）、
  default_template_id（meta template. 前缀）。
- 模板页：卡片默认徽章+设为默认/取消（POST /templates/default，**Form 参数不能用 EditParam**
  ——那是 Query 类型，Form 里读不到恒为默认值）；变量速查表。
- 每日笔记：**路由必须放 notes.py 且注册在 /notes/{note_id} 之前**（notes.router 比
  pages.router 先 include，放 pages.py 会被 422）；按标题找当天笔记，无则用「每日笔记」
  模板（按名字精确匹配）创建。编辑器「存为模板」按钮用 formaction 提交主表单。
- 坑：SYSTEM_PROMPT/模板类含 JSON 花括号要 {{}} 转义；测试查模板 id 用数据库别解析 HTML。
- 测试 709 → 722。

## 任务清单点击回写（17:40–17:55 提交 4b6585c）
- 渲染层 _mark_task_checkboxes：**disabled 的 input 会吞掉所有鼠标事件**（click 根本
  不派发），所以必须去掉 disabled 由 JS 接管；checked 与未 checked 的写法不同
  （`disabled checked/>`），替换模式用 `disabled(?=[ /])` 否则漏掉一半。
- markdown_render.toggle_task_item(content, index)：_TASK_MARK_RE（列表项 `[ xX]`）
  经 map_outside_code 处理，代码块外计数与切换，越界返回 None。
- POST /notes/{note_id}/task-toggle（登录+CSRF），repo.update_note(reason="task-toggle")
  版本历史兜底。detail.html 容器加 data-task-note；app.js document 级接管：
  详情页回写、其它页面（博客）preventDefault 只读。
- 真浏览器 e2e：点击→勾选→刷新保持→DB 确认。测试 722 → 734。

## N+1 性能优化（18:07–18:20）
- tags_page：tags[:12] 循环 list_notes → repo.notes_grouped_by_tags（ROW_NUMBER
  PARTITION BY t.name COLLATE NOCASE，排序 is_pinned/updated/id 与 list_notes 一致）。
- blog_archive：逐月 list_notes → repo.notes_grouped_by_months（PARTITION BY
  substr(updated_at,1,7)）。related_notes：循环内 _tags_map([单个id]) 合并为收尾一次。
- 关键：hydrate 会丢 SQL 别名列（_tag_name/_month），要先取出来再 zip hydrate 结果。
- 行为等价测试 9 例；实测 100 篇下标签页 2.1x。测试 734 → 743。

## 视觉美化（18:19–18:26）
- style.css 末尾「视觉美化打磨」补丁节：纸纹背景（radial-gradient 点阵+暖光，暗色单独写）、
  stat 左竖条、page-title 墨条、nav-link 下划线动效、主按钮渐变、::selection、
  细滚动条、表格行 hover、入场动效（**必须包 prefers-reduced-motion: no-preference**，
  否则会覆盖文件末尾的减弱规则）。
- color-mix() 现代浏览器 OK（Chrome 111+）。截图驱动：before/after 亮暗四图对比。
- 验证方式与 css_snapshot.py 相同：临时实例 + playwright 逐页截图。

## UI 打磨六项（18:37–18:50，按 docs/2026-09-13-ui-polish-plan.md 执行）
- ①热力图：stats_heatmap.build_heatmap（53×7、周一起始、今天末格、5 档色阶）；
  row-reverse 让最新周贴右；tooltip 纯 HTML title。
- ⑤单文件导出：note_export.build_export_html + GET /notes/{id}/export.html；
  Content-Disposition filename* 中文需 quote；detail 页「导出网页」按钮。
- ③专注模式：body.editor-focus 隐 header/footer/toolbar/settings，Esc + localStorage。
- 主题预设：data-palette（bamboo/ocean）与 data-theme 正交，各亮暗一组令牌；
  **坑：美化补丁 body 渐变硬编码锈红暖光，其它配色下会漏——改 color-mix 从 brand 派生**。
- 空状态插画：empty_art 宏 6 张内联 SVG，empty_state 自动替换（调用点零改动）。
- 骨架屏：.skeleton 组件落 agent pending 与 ask 首 token 前。
- 坑 1：disabled input 吞 click 之外又见 **auth_client 污染共享 client cookie**
  （未登录用例必须 fresh TestClient）；**session 库共享导致计数断言要相对化**。
- 坑 2：pytest 失败被 `| tail` 管道吞掉退出码，&& 链继续执行了提交——
  提交前判断测试结果别用管道尾部。
- 测试 743 → 752；手机视口 12 页 0 溢出。

## 配色扩充（19:16–19:24 提交 57a29cf）
- 新增霞紫(plum)/琥珀(amber)/石墨(slate)，共六套。机制全复用：data-palette 令牌 +
  防闪烁白名单 + nav-palette 按钮（app.js 数据驱动零改动）。
- getPropertyValue('--x') 返回 CSS 源文本（十六进制），不是计算后的 rgb——端到端断言别写 rgb()。

## 导航配色组挤压修复（19:24 提交）
- ≥1024 nav-more 是 display:contents——塞进菜单的任何新元素都会平铺进导航行！
  加宽元素前先想这个断点。配色组宽屏改 11px 色点（模板双内容 + CSS 切换），
  .site-nav .nav-link 加 nowrap+flex:none 根治逐字竖排。
- 竖排检测脚本：link 高度 > 46px 视为竖排（.scratch/nav_fix_check.py）。

## 配色切换器改下拉（19:27）
- 用户否掉色点方案。改原生 select（复用 .select--sm），change 事件驱动，
  ≥1024 行内紧凑 / <1024 菜单内整行。防闪烁机制不变。
- 遗留：style.css 里色点版规则（.nav-palette__dot/__text）成死代码，
  元素已不存在不会生效；下次 CSS 清理时可删。

## 配色切换器自定义下拉（19:32）
- 原生 option 放不了色块 → 自定义下拉（palette-dd：toggle+listbox 面板）。
- **坑：head 防闪烁脚本执行时 body 元素不存在**，querySelector('.palette-dd__label')
  为 null——label 恢复必须放 app.js ready 里。
- 交互完整：外点/Esc 收起、aria-expanded/aria-selected、动画期间截图会半透明（等 400ms）。

## 配色面板预览条（19:42）
- 「右边太空」指面板每行右半边空。每项加 34×14 渐变胶囊（brand→浅色，inline
  style 渐变写在模板），hover scaleX(1.12)，当前项 brand-soft 描边；勾移到条左。

## 菜单内配色控件风格（19:47）
- 用户指出 <1024 菜单里配色胶囊与其它菜单项不一致。改纯文字菜单项风格 +
  面板内嵌缩进（position:static 无框）。≥1024 行内胶囊不变。

## 配色控件全断点统一（19:50）
- 用户再次指出 1024+ 导航行内胶囊也不一致 → toggle 基础样式彻底改链接风格
  （无边框/透明/hover brand-soft），窄屏覆盖清理为只剩 width:100%。
- 验证法：computed style 对比 toggle 与普通 nav-link 的 border/bg/高度。

## 其余页面打磨（22:20）
- 404/错误页换 empty_art 插画；**footer 贴底**（body flex column + .site-main
  flex:1 0 auto，短页面不再 footer 悬空——截图确认）；编辑器写入 pane
  focus-within 顶部品牌色细条。
- 摸底结论：博客 hero/文章页/图片库/回收站/版本页均已达标，无需再动。

## 专注模式宽度修复（22:32）
- 760px 太窄被用户指出。现分栏 min(1160, 94vw) + 专注时 .site-main max-width:none
  （否则被 container 1120 限制）；单栏 :not(:has(split)) 收窄 900px。
- :has() 选择器用于模式区分（Chrome 105+）。

## 分栏拖拽（22:38）
- editor__split 三列 grid（minmax(0,var(--split,1fr)) 6px minmax(0,1fr)），
  --split=左占比/(1-占比) fr；pointer capture + body.split-dragging 禁选中；
  单栏/窄屏把手 display:none 并重申单列（补丁节里旧规则会被覆盖，重申顺序要对）。

## 快速复制 + 标签着色（22:47–22:55）
- 复制数据用 <script type="application/json"> 内嵌（CSP 安全），
  **必须放 block 内**——extends 模板 block 外的裸内容不渲染。
- tag_color filter：md5 稳定哈希 % 8（不能用 python hash，PYTHONHASHSEED 不稳）。
- 8 组 HSL 柔和色亮暗各一套；is-active 选中态品牌色优先。
- playwright 验证剪贴板：context permissions 授权 clipboard-read/write。

## 那年今日 + 归档（23:01–23:10）
- 那年今日：substr(created_at,6,5)=今日 && 年份<今年，列表页卡片（非搜索/非归档视图）。
- 归档：MIGRATIONS 加列 is_archived；list_notes archived 三态（None/False/True），
  SQL 与 q 搜索 Python 分支都要过滤；set_archived 独立函数（不进 FLAGS 白名单）；
  POST /notes/{id}/archive toggle；detail 按钮 + 列表「已归档」入口 + 归档视图空状态。
- 坑：URL 里 msg 是编码的，断言要 unquote_plus；session 库共享用例间污染（清表）。

## 用量统计补全（23:00–23:27 提交 e81ed96）
- summary 加 by_model（本月按模型拆分，token 降序）+ all_calls/all_tokens/since 累计。
- 设置页：「按模型拆分（本月）」+ 品牌色占比条 + 累计行；空月提示也带历史累计。
- **坑：裸 python 验证写进了真实 data/inknote.db 的 ai_usage 表**（没设 INKNOTE_DATA_DIR
  且没走 venv）——已清理 test-model 记录。任何验证必须 venv python + 隔离数据目录。
- 全量测试后台跑（用户要求加速）。

## 用量柱状图增强（23:32–23:38）
- 柱高改按 token（原按次数）；堆叠输入/输出双色段（prompt_pct 计算）；
  柱顶 number 数值（**number 输出千分位**，断言写 1,500）；近 14 天明细表。
- 一次性脚本三连坑：没 base 定义、ai_usage 惰性建表要先 ensure、裸 python 不可用。
