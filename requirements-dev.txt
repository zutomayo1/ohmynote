-r requirements.txt

# 开发 / 自测依赖 —— pip install -r requirements-dev.txt
pytest>=8.0
httpx>=0.27                  # starlette TestClient 依赖
pytest-xdist>=3.5            # 并行跑测试：pytest tests -n 8 --dist loadfile（约 194s → 24s）
pytest-cov>=5.0            # 覆盖率：pytest tests --cov=app --cov-report=term
