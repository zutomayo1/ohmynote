# 墨痕 InkNote · 使用说明

> 一个自己用的网页笔记工具：**用 Markdown 写笔记，勾一下就能发成博客文章**。
> 数据全部存在你自己电脑上（`C:\repo\inknote\data` 文件夹），不联网、不上传、不需要注册账号。

> 💡 这份文档就是项目里的 `使用说明.md`。除了用编辑器打开它，**在网站上点页脚的「使用说明」**（地址 `/manual`）也能看到排版好的版本。

> 🆕 **本次更新**（2026-09 中旬）新增了这些东西，文档已同步：
> **关系图谱**（把 `[[双链]]` 画成一张网）、**命令面板**（`Ctrl+K` 输拼音就能跳转）、
> **模板增强**（AI 生成模板 / 拖拽排序 / 全部模板可折叠）、**博客阅读进度条**、
> **首次使用引导**、**星标·置顶·公开改为点击即时生效**（不再整页刷新）、
> **笔记助手的批量操作与删除前确认卡片**，以及一批界面动效（详见第 17、18 节）。

---

## 目录

1. [怎么打开](#一怎么打开)
2. [第一次使用](#二第一次使用)
3. [页面地图：每个地方是干嘛的](#三页面地图每个地方是干嘛的)
4. [怎么写一篇笔记](#四怎么写一篇笔记)
5. [Markdown 语法速查](#五markdown-语法速查)
6. [标签、分类、搜索、双链](#六标签分类搜索双链)
7. [发布成博客](#七发布成博客)
8. [改错了怎么办：历史版本与回收站](#八改错了怎么办历史版本与回收站)
9. [快捷键与命令面板](#九快捷键与命令面板)
10. [深色模式与手机上看](#十深色模式与手机上看)
11. [备份与搬家](#十一备份与搬家)
12. [常见问题](#十二常见问题)
13. [进阶：AI 功能](#十三ai-功能可选)
14. [批量操作、站点设置、图片管理](#十四批量操作站点设置图片管理)
15. [自动备份、改密码、批量全选](#十五自动备份改密码批量全选)
16. [日志、导出图片、更多细节](#十六日志导出图片更多细节)
17. [关系图谱](#十七关系图谱)
18. [界面动效与首次引导](#十八界面动效与首次引导)

---

## 一、怎么打开

**双击桌面上的「墨痕笔记」图标**，就这一下。

接下来会自动发生三件事：

1. 弹出一个黑色窗口（这是网站程序本体，**用的时候别关它**）
2. 等 3～5 秒，浏览器自动打开 <http://127.0.0.1:8000/>
3. 点右上角「登录」，密码输入 `inknote`，回车

**用完想关掉**：把那个黑色窗口关掉就行。

> 桌面没有图标？打开文件夹 `C:\repo\inknote`，双击里面的 **`启动笔记.bat`**，效果一样。
>
> 服务已经在跑的时候又双击一次图标也没关系 —— 它不会报错，会直接帮你打开浏览器。

---

## 二、第一次使用

登录后你看到的是**笔记列表**（空的）。建议这样开始：

1. 点右上角蓝色的「**写笔记**」
2. 标题随便写，比如「我的第一篇笔记」
3. 正文写两句话
4. 点右下角「**保存并查看**」

看到自己的笔记页面就成了。

> 第一次进站点时，页面上会自己走一遍**四步引导**（写笔记 → 快速搜索 → 标签筛选 → 深色模式），
> 看完一次就永不再出现。想跳过直接按 `Esc`，细节见第 18 节。

> 想写今天的日记？导航「更多 → **今日笔记**」一键直达：今天的笔记存在就打开，不存在就自动建好（如果模板页里有名叫「每日笔记」的模板，会自动套用）。

> 想快速试各种功能？点「**模板**」→ 里面内置了 6 个模板（空白笔记、读书笔记、周报、会议记录、技术笔记 / 踩坑记录、每日复盘），点「用这个模板」就能带着格式开始写。

---

## 三、页面地图：每个地方是干嘛的

### 顶部导航（登录后）

左边三个是常驻入口：

| 菜单 | 干什么 | 地址 |
| --- | --- | --- |
| **笔记** | 你的全部笔记（含草稿），主战场 | `/notes` |
| **标签** | 所有标签 + 按标签分组的笔记 | `/tags` |
| **博客** | 对外展示的公开页面，**不需要登录** | `/blog` |

其余的收在右边的「**更多 ▾**」里：

| 更多菜单 | 干什么 | 地址 |
| --- | --- | --- |
| **模板** | 管理笔记模板：新建 / **AI 生成** / **拖拽排序** / 设为默认 / 支持日期变量 | `/templates` |
| **问笔记** | 检索你的笔记再让 AI 作答，可连续追问、流式输出、一键存成笔记（见第 13 节） | `/ask` |
| **笔记助手** | 用一句话让它替你动手改笔记，执行过程实时可见（见第 13 节） | `/agent` |
| **今日笔记** | 一键打开今天的日记，没有就自动建好 | `/notes/today` |
| **今日待办** | 把各篇笔记里的待办清单汇总到一页勾选 | `/todos` |
| **写作统计** | 字数、年度热力图、写作节奏 | `/stats` |
| **关系图谱** | 把 `[[双链]]` 画成一张网，看笔记之间的脉络（见第 17 节） | `/graph` |
| **配色**（菜单最底部） | 切换六套配色（见第 13 节） | — |
| **回收站** | 删掉的笔记先放这里，30 天内可恢复 | `/trash` |

除此之外，**页面最底部**还有几个入口：

| 页脚入口 | 干什么 | 地址 |
| --- | --- | --- |
| **使用说明** | 就是这份文档，排版好直接在浏览器里看 | `/manual` |
| **设置** | 站点信息（站名 / 副标题 / 每页条数 / 回收站天数）+ 配置 AI 服务 + 改密码 | `/settings` |
| **备份与恢复** | 导出全部笔记、把导出的 zip / json / md **导回来**、整库备份与回滚 | `/backup` |
| **图片** | 上传过的图片：被哪篇笔记引用、清理没用的图 | `/images` |
| **导出全部笔记** | 打包下载全部 Markdown | `/export/zip` |

### 笔记列表页

- 顶部四个数字：**累计字数 / 连续写作天数 / 今日更新 / 标签数**（就是个小激励面板）
- 搜索框：搜**标题 + 正文 + 标签**，多个关键词用空格分开（要同时命中）
- 「全部状态 / 排序 / 全部分类」三个下拉框：筛选和排序
- 一排小胶囊：`全部` `★ 星标` `置顶` `已公开` 快速过滤；后面是**标签胶囊**，点一下就按标签筛
- 每张卡片上有一排小图标按钮：✏️编辑、📌置顶、★标星、👁公开到博客、🗑删除
- 卡片上的徽章会告诉你这篇是「**草稿**」还是「**已保存**」，以及是否「已公开 / 置顶 / 星标」
- **置顶 / 标星 / 公开这三下点完就生效**，页面不会整页重刷（按钮当场变色）；标星点亮时会有一记金色的小弹跳
- **置顶的笔记可以拖动排序**：抓住置顶卡片左上角的 `⠿` 拖到新位置（或聚焦手柄后按 `Alt+↑` / `Alt+↓`），顺序自动保存；只影响置顶笔记之间的先后，非置顶笔记不受影响
- 点删除会先问一句「正文引用了 N 张图片」，确认后**卡片缩扁淡出**再跳转 —— 看得见它确实被删掉了

### 笔记详情页（点标题进去）

左边是正文，右边一栏（手机上会跑到下面）依次是：

- **目录**：正文里的标题会自动生成，滚动时会高亮当前章节，点一下跳过去
- **你可能还想看**：根据标签重合度自动推荐的相关笔记（会说明推荐理由）
- **反向链接**：哪些笔记里写了 `[[当前这篇的标题]]`
- **指向**：这篇笔记引用了别人；如果目标还不存在，会显示虚线 + 一个「创建 →」入口，点一下就能新建
- **局部关系图**：这篇笔记的邻居（默认 1 跳，可切「看 2 跳」），点圆点直接跳过去
- **最近历史**：最近几个版本快照

右上角按钮：**编辑**、**公开到博客 / 取消公开**、**置顶**、**标星**、**历史**、**导出单篇**、**在博客中查看**、**移入回收站**。
其中**置顶 / 标星 / 公开**点完即时生效、不刷新页面；失败会弹提示并把按钮状态退回去，不会出现"看着开了其实没开"。

> 详情页顶部的细线是**阅读进度条**（长文章才有，读完淡出），博客文章页、使用说明页也有。

---

## 四、怎么写一篇笔记

编辑器从上到下是这样：

```
┌────────────────────────────────────────────┐
│  标题输入框        编辑 | 分栏 | 预览   保存状态 │
├────────────────────────────────────────────┤
│  B  I  S | H1 H2 H3 | 列表 待办 引用 | 代码 …  │  ← 工具条
├──────────────────────┬─────────────────────┤
│                      │                     │
│   左边：写 Markdown    │   右边：实时预览       │
│                      │                     │
├──────────────────────┴─────────────────────┤
│  字数 · 阅读时长 · 第几行      支持拖拽/粘贴图片  │
├────────────────────────────────────────────┤
│  ▸ 发布设置（标签、分类、摘要、slug、公开开关…）  │
├────────────────────────────────────────────┤
│            存为草稿   保存   保存并查看         │
└────────────────────────────────────────────┘
```

三个要点：

1. **三个视图**：`编辑` 只看写作区、`分栏` 左写右看（宽屏推荐）、`预览` 只看效果。手机上默认只显示写作区。
2. **自动保存**：**已经在编辑的笔记**停笔 2.5 秒会自动存一次，右上角会显示「保存中… / 已保存」。
   **新建的笔记不会自动保存**（还没有 ID），一定要点「保存」。
3. **发布设置**（默认展开的折叠区）：

| 字段 | 说明 |
| --- | --- |
| 标签 | 用逗号或空格分隔；正文里写的 `#标签` 也会自动收集进来 |
| 分类 | 比如「技术 / 读书 / 生活」，用来在列表页筛选 |
| 摘要 | **留空就自动截取正文开头**；填了就一直用你填的（列表页和博客卡片会显示这句） |
| slug | 网址路径，比如 `python-asyncio`；留空自动按标题生成，中文标题也能当 slug |
| meta 描述 | 给搜索引擎看的，留空就用摘要 |
| 公开到博客 | 勾上就出现在博客里（勾上后状态自动变「已保存」） |
| 置顶 / 星标 | 置顶排最前，星标方便快速筛选 |

底部三个按钮的区别：

- **存为草稿**：标记成草稿，写完还可以继续改（不进博客）
- **保存**：标记成「已保存」，留在编辑器里继续写
- **保存并查看**：保存后跳到详情页看最终效果

### 插图

三种方式都行，图片会存到你本地的 `data/uploads` 里：

- 直接把图片**拖进**编辑区
- 直接 **Ctrl+V 粘贴**截图
- 点工具条上的「图片」按钮选文件

支持 `png / jpg / jpeg / gif / webp / bmp / avif`，单张最大 8MB。上传后会插入一行 `![名字](/media/2026/09/xxx.png)`。多张图会一张张依次上传。

---

## 五、Markdown 语法速查

> 记住一条就行：**标题的 `#` 后面必须有空格**。`# 标题` 是标题，`#标题` 是普通文字（这一条是为了让正文里的 `#标签` 不被吃成标题）。

| 想要的效果 | 这么写 |
| --- | --- |
| 一级标题 | `# 标题` |
| 二级标题（**推荐**，会进右侧目录） | `## 小标题` |
| 三级标题 | `### 更小的标题` |
| **加粗** | `**加粗**` |
| *斜体* | `*斜体*` |
| ~~删除线~~ | `~~删除线~~` |
| 高亮 | `==高亮==` |
| 行内代码 | `` `代码` `` |
| 代码块（带语法高亮） | 用三个反引号围起来，第一行后面写语言名，比如 python |
| 无序列表 | `- 一项` |
| 有序列表 | `1. 一项` |
| **待办清单** | `- [ ] 没做` / `- [x] 做了` |
| 引用 | `> 引用一段话` |
| 分割线 | `---` |
| 链接 | `[文字](https://example.com)` |
| 图片 | `![说明](https://example.com/a.png)` |
| 表格 | 见下面 |
| 脚注 | 正文写 `[^1]`，末尾写 `[^1]: 注释内容` |
| 双链 | `[[另一篇笔记的标题]]` |
| 标签 | `#技术` |
| 任务清单 | `- [ ] 待办` / `- [x] 已完成` —— **阅读视图里点复选框就直接打勾并保存**（博客公开页只读） |
| 任务清单 | `- [ ] 待办` / `- [x] 已完成` —— **阅读视图里点复选框就直接打勾并保存**（博客公开页只读） |
| **数学公式** | `$$E = mc^2$$`（行间）或 `\(a+b\)`（行内）；**单个 `$` 不算公式**，写价格不用转义 |
| **流程图 / 时序图** | 代码块语言写 `mermaid`，例如 ` ```mermaid ` 开头，正文写 `graph LR` / `A --> B` |

表格写法（第二行的 `---` 是必须的）：

```markdown
| 项目 | 说明 |
| --- | --- |
| 后端 | FastAPI |
| 数据库 | SQLite |
```

代码块写法：第一行三个反引号后面写语言名，最后单独一行三个反引号收尾。下面用四个反引号把例子包起来，好让你看清里面那三个反引号：

````markdown
```python
def hello(name):
    return "hi " + name
```
````

支持的语言名：`python` `javascript` `bash` `json` `yaml` `sql` `html` `css` `go` `rust` `java` `text` 等几十种，代码块会按语言着色。

**不支持**在正文里写 HTML 标签（会被当成纯文本显示，这是为了安全）。

---

## 六、标签、分类、搜索、双链

### 标签

两种打标签的方式，可以混用：

1. 在编辑器的「发布设置 → 标签」里填，用逗号或空格分隔：`python, 异步, 踩坑`
2. 直接在正文里写 `#异步`（注意 `#` 后面不要空格）

代码块和行内代码里的 `#xxx` 不会被当成标签。

编辑器里还有两个顺手的：输入框下面会列出你**最常用的标签**（点一下直接加/去掉），
以及边打字边给出的**自动补全**建议。

#### 标签页（`/tags`）与管理入口

导航栏「标签」进去就是标签页：上面是标签云，点某个标签就筛出用它标注的笔记，
下面按标签分组列出最近的笔记。右上角可以**搜索标签**、**按名称/使用次数排序**。

标签药丸**每个都有自己的颜色**（8 组色相，同一标签在哪儿都是同一个色），后面跟一个圆角小徽标写着**用了几次**。
鼠标移上去描边加深、轻轻浮起；点中那个标签会变成实心高亮 —— **笔记列表页的标签筛选用的是同一套样式**，两处看起来完全一致。

页面上方有一条「**管理标签**」（默认收起，点开就行）：

| 想做的事 | 怎么做 |
| --- | --- |
| 改写标签名 | 「要改名 / 合并的标签」选一个，填新名字 → 重命名 / 合并。用到它的笔记会一起改 |
| 合并两个标签 | 同上，新名字直接填**已经存在的那个标签**——关系会取并集，不会出现重复标签 |
| 删掉一个标签 | 展开任意标签胶囊旁的 **✎** → 「🗑 删除」。会把所有笔记上的这个标签摘掉（笔记本身不动） |
| 清理没用的标签 | 「管理标签」里的「清理未使用的标签」 |

> 删除只是**摘掉标签**，不会删笔记；删之前会让你确认一次。

### 搜索

三个入口，随便用哪个：

- 列表页顶部的搜索框：搜标题 + 正文 + 标签，**标题命中的排最前面**
- 顶部导航栏的 🔍 图标，或按 **Ctrl+K**：打开**命令面板**（见第 9 节）——没输入时列出一排常用命令，
  输入关键词就同时搜命令和笔记，↑↓ 选、回车跳转
- 搜索结果页 `/search`：命中词会高亮

搜索支持轻量语法（每个入口都能用，语法可以和普通关键词混用）：

| 写法 | 作用 |
| --- | --- |
| `tag:读书` 或 `#读书` | 只搜带这个**标签**的笔记 |
| `title:周报` | 只匹配**标题** |
| `cat:技术` | 只搜这个**分类**下的笔记 |
| `"精确短语"` | 引号里的内容整体匹配，不被拆词 |
| `-草稿` | 排除包含这个词的笔记 |
| `after:2026-01` / `before:2026-09` | 按「最近更新」时间过滤（支持年 / 年-月 / 年-月-日） |
| `is:starred` / `is:pinned` / `is:public` / `is:draft` | 只看星标 / 置顶 / 已公开 / 草稿 |

比如 `部署 tag:技术 -草稿` = 分类「技术」里、不含「草稿」、提到「部署」的笔记。
中文搜索不用担心分词：一个字、两个字、长词、词中间的字都能搜到（全文索引会自动切成字与相邻二字）。

搜索结果页上还有一个「**按意思搜**」开关（默认关闭）。打开后，就算你用的词和笔记里的字面不一样，
也能按语义召回相近的笔记——前提是你已经在设置页配好 embedding 模型并建过索引。
没配好时它会自动退回关键词搜索，并在结果上方**告诉你这次用的是哪种引擎**。

搜索结果页的结果卡片和列表页一样带操作按钮（星标 / 置顶 / 公开 / 编辑 / 删除），不用跳回列表再操作；
卡片上方还有一排筛选：按**星标 / 置顶 / 已公开 / 草稿**、**标签**、**分类**、**状态**缩小范围，
或按最近更新 / 创建时间 / 字数 / 标题排序。不筛就保持相关度顺序，最相关的排最前。

> 搜索是「所有关键词都要命中」的逻辑。比如搜 `异步 循环`，两篇里分别出现这两个词但同一篇没有同时出现，就不会算命中。

### 双链（把笔记连起来）

在正文里写 `[[另一篇笔记的标题]]`，就会变成一个链接；想显示别的文字就写 `[[另一篇笔记的标题|显示成这个]]`。

连起来之后有四个好处：

- 点链接直接跳过去
- **目标笔记**的详情页会显示「反向链接」：哪些笔记提到了它
- 目标还不存在时，链接显示成虚线，旁边给个「创建 →」，点一下用它当标题新建
- 「你可能还想看」会把互相引用过的笔记推荐出来

#### 怎么快速建立链接（不用背标题）

三条路，都不需要你记住对方的完整标题：

| 在哪 | 怎么做 |
| --- | --- |
| **编辑器正文里** | 打两个左方括号 `[[` → 就地弹出标题候选，再打几个字收窄范围，`↑` `↓` 选、`Enter`（或 `Tab`）插入完整的 `[[标题]]`，`Esc` 取消 |
| **编辑器工具栏** | 点「`[[ ]]`」按钮：自动打上 `[[` 并直接打开候选框 |
| **笔记页右栏「指向」区块** | 底部有「输标题，连接另一篇笔记…」输入框：打字选一篇 → 点「连接」。链接会**加在正文末尾**（正文里会多出一行 `[[标题]]`） |

> 「局部关系图」下面那行也有个「**＋ 建立联系**」，点一下直接跳到这个输入框。

**想断开**：「指向」列表里每条右边的 **✕**，点一下就删掉正文里对应的那处 `[[链接]]`
（只删链接文字，其它正文不动）。删错了去「历史」回滚 —— 建立和断开都会留版本快照。

**目标笔记还没建**：先在「指向」里点「创建 →」，新建完回来再连；或者直接连，服务端会告诉你没找到。

> **改了标题怎么办**：保存后如果发现还有别的笔记引用着旧标题，编辑页顶部会出现一条提示——
> 点「一并更新」，那些笔记正文里的 `[[旧标题]]`（含别名写法）会自动改写成新标题，每一篇都留版本快照。
> 例外：如果新标题和另一篇笔记撞名，会先提醒你换个名字，避免链接改到错误的笔记上去。
>
> 没有 JavaScript 也能用：正文里手打 `[[标题]]` 照旧；「建立联系」输入框直接打标题提交，
> 服务端按标题找 —— 匹配到多篇时会把候选列出来，让你写得更完整一点。

---

## 七、发布成博客

1. 打开笔记 → 点「**公开到博客**」（或者在编辑器里勾「公开到博客」）
2. 访问 <http://127.0.0.1:8000/blog> 就能看到了，**这个页面不需要登录**，可以直接把网址发给别人（前提是对方能访问你的电脑，见下面第 12 节的「怎么让手机/别人看」）

博客页面有什么：

| 页面 | 地址 | 内容 |
| --- | --- | --- |
| 博客首页 | `/blog` | 文章卡片流、标签筛选、按月在底部归档 |
| 文章页 | `/blog/文章路径` | 正文 + 目录 + 相关文章 + **上一篇 / 下一篇** |
| 归档 | `/blog/archive` | 时间轴列出全部公开文章（按发布时间倒序、年份分节、条目滚动淡入） |
| 标签 | `/blog/tags` | 全部标签 |
| 订阅源 | `/feed.xml`、`/rss.xml` | Atom / RSS，可直接丢进 RSS 阅读器订阅 |
| 关于 | `/about` | 站点介绍 |

**SEO 小细节**：每篇文章都能单独设置 `slug`（网址路径）和 `meta 描述`（搜索结果里显示的介绍），页面里也自动输出了 `canonical`、Open Graph 标签，还有 `sitemap.xml` 和 `robots.txt`。

**阅读进度条**：博客文章页顶部有一条细线，跟着你往下滚一点点长，读到结尾就淡出。
文章短到不用滚的不会显示。笔记详情页、使用说明页也有同一条。

**阅读体验**（博客与笔记详情页共用）：

- **代码块**：悬停时右上角出现「复制」按钮、左上角显示语言标签（如 `python`）
- **图片灯箱**：点击正文里的图片放大居中看，点空白或按 `Esc` 关闭（已包链接的图除外）
- **目录高亮**：往下滚时，侧栏目录自动点亮你正在读的小节
- **点赞与阅读数**：文章末尾的「觉得有用，点个赞」按钮，同一访客只记一次（记住在浏览器里），
  旁边显示阅读次数；作者自己打开文章不计数
- **首页动效**：文章卡片滚入视口时淡入上移（系统开了「减少动态效果」则自动关闭）

**草稿不会泄露**：没公开的笔记不会出现在博客、RSS、搜索里；公开页面里的 `[[双链]]` 也只指向已公开的笔记。

---

## 八、改错了怎么办：历史版本与回收站

### 历史版本（防手滑改坏）

**每次保存前**，系统会自动把「改动之前的样子」存一份快照：

- 点详情页的「**历史**」按钮 → 看到一列版本，每条显示时间、摘要和类型
- 点「**查看差异**」→ 逐行对比这一版和当前内容的区别（绿色是新增、红色是被改掉的）
- 点「**回滚**」→ 一键回到那一版（回滚前也会先把当前内容存一份，所以回滚本身也能反悔）

默认保留最近 **20** 个版本。连续打字时的自动保存不会刷屏：10 分钟内的自动快照会合并成一条。

### 回收站（防删错）

- 删除笔记**不是真删**，而是进回收站（`/trash`），可以随时「恢复」
- **30 天**后才会彻底清除（程序启动时会检查一次）
- 想立刻清掉就点「彻底删除」，或者右上角「清空回收站」

---

### 两个小提示

- 导航里的「**回收站**」会带一个**数字角标**，提醒你里面还压着几篇（清零后角标自动消失）
- 列表页每张卡片右下角的 **🕐 历史** 图标，可以直接跳到这篇笔记的版本历史，不用先进详情页

## 九、快捷键与命令面板

| 快捷键 | 作用 | 在哪用 |
| --- | --- | --- |
| `Ctrl + K` | 打开**命令面板**（命令 + 搜索笔记） | 除了编辑器正文之外的任何地方 |
| `Ctrl + K` | 插入链接 `[文字](网址)` | 编辑器正文里 |
| `/` | 打开命令面板 | 不在输入框里时 |
| `Ctrl + S` | 立即保存 | 编辑器 |
| `Ctrl + B` / `Ctrl + I` | 加粗 / 斜体 | 编辑器 |
| `Ctrl + Enter` | 保存并查看 | 编辑器 |
| `Tab` | 缩进两个空格（不会跳出输入框） | 编辑器正文 |
| `Esc` | 关掉搜索面板 / 收起发布设置 | 全局 |
| `↑` `↓` `Enter` | 在命令面板里选择、打开 | 命令面板 |
| `Ctrl + Enter` | 在命令面板里看全部搜索结果 | 命令面板 |
| `J` / `K`（或 `↓` `↑`） | 选中下一条 / 上一条笔记 | 笔记列表、搜索结果页 |
| `Enter` | 打开选中的笔记 | 同上 |
| `X` | 勾选 / 取消选中（配合批量操作） | 笔记列表 |
| `?` | 呼出**快捷键速查**浮层 | 任何页面 |

> 在命令面板里打关键词，标题和摘要里命中的字会高亮；直接回车跳转，`Ctrl+Enter` 去完整的结果页。
> 任何页面按 `?` 都能弹出快捷键速查，不用背这张表。

### 命令面板（Ctrl + K）

`Ctrl+K` 打开的那个面板现在不只是搜索，而是一个**命令面板**：

- **不输入任何字** → 直接列出常用命令：新建笔记 / 今日待办 / 关系图谱 / 写作统计 / 模板 /
  回收站 / 切换深色模式 / 打开设置，点一下就执行（结果排在「命令」分组下）
- **打字** → 命令和笔记**混排**（命令在前、笔记在后，各带分组标题），关键词同时匹配两边
- **支持拼音**——不用切换输入法打中文：输 `xjbj` 是「新建笔记」、`txp` 是「关系图谱」、
  `qhsmsm` 是「切换深色模式」；也可以打全拼（`guanxitupu`）
- **最近打开过的笔记**会出现在下面，接着看很方便
- 搜不到东西时它会说一句「没找到」，而不是给个空白框

---

## 十、深色模式与手机上看

- **深色模式**：点右上角的 🌙 图标切换。配色是深棕底 + 米白字（不是刺眼的白底黑字反过来），会记住你的选择；第一次访问时跟随系统设置。刷新页面不会闪白。
- **响应式**：手机上也能用 —— 导航自动换行、笔记卡片变单列、编辑器自动变成单栏（编辑 / 预览用顶部 tab 切）。
- **打印**：`Ctrl+P` 打印时会自动隐藏导航、按钮、目录，只留标题和正文。

---

## 十一、备份与搬家

你所有东西都在这个文件夹里：

```
C:\repo\inknote\data\
├── inknote.db       ← 全部笔记、标签、历史版本、模板（SQLite 数据库）
├── .secret          ← 登录用的密钥，别删也别人给别人
└── uploads\         ← 你上传的图片，按 年/月 分目录
```

### 备份

**最简单**：把整个 `data` 文件夹复制一份到别处（U 盘、网盘、另一个盘）。

> 建议先把黑色窗口关掉（停掉服务）再复制，这样最稳。

**或者**用页面里的导出（推荐偶尔做一次，导出来的是人能直接看的文件）：

- 笔记列表页右上角「**导出**」按钮 → 下载一个 zip，里面是：
  - `index.md`（目录）
  - `notes/` 文件夹：每篇笔记一个 `.md` 文件，开头带标题、日期、标签等元信息
  - `notes.json`（结构化元数据）
- 单篇导出：详情页的「⬇」按钮，或者访问 `/notes/编号/export.md`

导出的是**标准 Markdown 文件**，不依赖这个工具，可以直接拖进 Obsidian、Typora、VS Code 或任何笔记软件打开。

### 搬家到另一台电脑

1. 把整个 `C:\repo\inknote` 文件夹复制过去
2. 在新电脑上装好 Python 依赖：在文件夹里执行一次 `pip install -r requirements.txt`
3. 双击 `启动笔记.bat`

---

### 导回来（恢复 / 合并）

导出的 zip 和 json **可以导回来**，在页脚点「**备份与恢复**」（`/backup`）：
拖入文件后虚线框里会先列出待导入清单（文件名 / 类型 / 大小，可单个移除），确认无误再点「开始导入」。

1. 把导出的 `.zip`、`.json` 或单篇 `.md` **拖进方框**（或点一下选文件）
2. 点「导入」→ 它按**标题 / slug** 认出是哪篇笔记：
   - 已经有这篇 → **更新**它
   - 没有 → **新建**
   - **不会删除**你现有的、备份里没有的笔记
3. 页面会告诉你「新建 N 篇 · 更新 M 篇 · 跳过 K 篇」，有问题的文件会单独列出来

同一份备份导两次不会变成两份（第二次全是「更新」）—— 这点实测过。

> 什么情况下用得上：换了台电脑、`data` 文件夹误删、或者把几年前的 md 笔记一次性搬进来。

## 十二、常见问题

**Q：浏览器显示「无法访问此页面 / 拒绝连接」**
说明网站程序没在跑。双击桌面「墨痕笔记」图标启动它。注意：**那个黑色窗口不能关**，关了网站就停了。

**Q：双击图标后黑色窗口一闪就没了**
说明启动时报错了。用下面方法看到报错：在文件夹里按住 `Shift` 右键 → 「在此处打开 PowerShell 窗口」，输入：

```powershell
.\.venv\Scripts\python.exe run.py
```

把红字发我。

**Q：提示端口被占用（`address already in use`）**
说明已经有一个在跑了 —— 直接刷新浏览器即可；实在不行关掉所有黑色窗口，再双击一次图标。

**Q：密码忘了 / 想改密码**
先关掉黑色窗口，然后在文件夹里打开 PowerShell：

```powershell
copy .env.example .env
.\.venv\Scripts\python.exe run.py --hash-password '你的新密码'
```

把输出的那行 `pbkdf2_sha256$...` 填进 `.env` 文件的 `INKNOTE_PASSWORD_HASH=`，并把 `INKNOTE_PASSWORD=` 这行前面加 `#` 注释掉，保存后重新双击图标启动。

**Q：怎么改网站名字 / 副标题 / 每页显示几篇**
改 `.env` 文件（没有就先把 `.env.example` 复制成 `.env`）：`INKNOTE_TITLE`、`INKNOTE_SUBTITLE`、`INKNOTE_PER_PAGE`… 里面有中文注释，改完重启。

**Q：怎么让手机或者别人也能看（局域网）**
把启动命令换成监听所有网卡：在文件夹里开 PowerShell 执行

```powershell
.\.venv\Scripts\python.exe run.py --host 0.0.0.0
```

然后在手机上访问 `http://电脑的局域网IP:8000`（IP 用 `ipconfig` 查）。注意：这样**同一网络下的人都能打开你的博客页面**（笔记后台仍然需要密码）。

**Q：会不会把我的笔记传到网上去？**
不会。程序只在你电脑上跑，数据只写进 `data` 文件夹。唯一会联网的情况是你**主动配置并点击了 AI 功能**（见下一节）。

**Q：`/docs` 这个页面是什么？**
FastAPI 自动生成的接口文档。**默认已经关闭**（`/docs`、`/redoc`、`/openapi.json` 都打不开）——
这是私人笔记应用，没必要向匿名访客暴露 API 结构。里面的**写接口本身仍然要登录 + CSRF**。
调试接口时想打开它，在 `.env` 里加一行 `INKNOTE_DOCS=1` 再重启即可。

---

## 十三、AI 功能（可选）

配好之后，编辑器里会多出四个按钮，「问笔记」也能给出用 AI 总结的回答。
**不配也能正常用**，只是点了会提示「尚未配置」。

### 在网页上配（不用改任何文件）

点页面最底部的「**设置**」，或直接访问 <http://127.0.0.1:8000/settings>：

1. **点一个预设按钮**：免费的三个在前面——**硅基流动**（注册送额度，bge-m3 向量无限免费）、
   **智谱 GLM**（Flash 系列对话免费不限量）、**本地 Ollama**（离线，不用 Key）；
   DeepSeek / OpenAI / 通义千问 收在「更多服务商」折叠块里。
   点一下自动填好地址和模型名，旁边还给了「去哪申请 Key」的链接
2. 填 **API Key**（用本地 Ollama 不用填）
3. 点「**测试连接**」—— 它会分四步体检，断在哪一步写得很清楚：

   | 步骤 | 过了说明 |
   | --- | --- |
   | 连通性与密钥 | 地址能访问、Key 有效（顺便列出对方有多少个模型） |
   | 模型名 | 你填的模型确实存在；写错了它会告诉你有哪些可用 |
   | 对话 | 真的发一句过去，模型正常回话 |

4. 点「**保存配置**」→ 状态变成「**已启用**」，立刻生效

### 几个进阶设置

| 设置 | 作用 |
| --- | --- |
| **获取可用模型** | 按钮，拉取服务商支持的模型列表做成下拉，不用手打模型名 |
| **分任务模型** | 摘要、打标签、问答可以各用不同模型 —— 比如摘要用便宜的小模型，问答用强的，省钱 |
| **向量模型** | 填 embedding 模型名后，「问笔记」就能**按意思检索**（见下）；留空则用关键词检索 |
| **发送前脱敏** | 打开后，正文里像 `sk-xxxx`、`password=xxx`、手机号、身份证号会先打码再发给 AI |
| **只允许本机模型** | 打开后只允许 `127.0.0.1` 之类的本地地址，防止哪天填错地址把笔记发到外面去 |

### 用量统计

设置页「用量统计」里能看到的维度：

- **四个窗口**：今日 / 本周（周一起）/ 本月 / 累计，以及**本月 vs 上月的环比**（涨了多少一眼看到）
- **质量**：成功率（失败几次）、平均与最慢耗时、平均与单次最大的 token —— 慢在哪、错在哪有据可查
- **最近 20 次调用明细**：时间 / 任务 / 模型 / 输入 / 输出 / 耗时，失败的标红；另有「最近的失败」列出错误原因
- **按任务**：问笔记、问笔记（流式）、笔记助手、摘要、打标签…**分开归因**，
  能看出到底是哪类调用把额度吃掉了（以前助手和问笔记混在一起，看不出来）
- **按模型**、**模型 × 任务**：哪个模型在干什么活、谁最费
- **时间规律**：24 小时的分布 + 一周里哪天用得多（近 30 天）
- **近 6 个月趋势** + **近 30 天柱状图**（输入/输出堆叠）+ **近 14 天明细表**
- **导出 CSV 明细**：拿去做透视表、算钱都行；还有「重置统计」

图表都能**悬停看明细**：把鼠标放到任意一根柱子（或横条）上，会浮出一张小卡片，
写着那一格的调用次数、输入 / 输出 / 合计 token、失败次数和平均耗时 ——
这些是页面上没直接列出来的（比如「哪天失败过」「当天平均多慢」）。
键盘也能用：把焦点移到图表上（Tab 到图表），左右方向键逐格移动、Esc 关闭。
展开的浮层样式和柱子的两段同色，所以「提示里的色条」和「柱子里的色块」能对上。

一些说明：耗时是从这次升级之后才开始记的，之前的调用没有这一项；
失败也会记一笔（这正是失败率的来源），所以调用次数比「只数成功」时略多。
花钱的东西得能看见。

### 向量检索（推荐开）

默认的「问笔记」是**按字面**找笔记：你问「缓存失效怎么处理」，而笔记里写的是「Redis TTL 踩坑」——
一个字都对不上，就搜不到。

填上**向量模型**（比如 OpenAI 的 `text-embedding-3-small`、通义的 `text-embedding-v3`、
本地 Ollama 的 `nomic-embed-text`），然后在设置页点「**重建索引**」，
它会把你的笔记切成块、算成向量存到本地。之后提问就是**按意思找**：

- 命中会显示「**语义检索**」；没配或索引没建好时自动回退成「**关键词检索**」，页面上会标出来
- 换了模型、或者笔记改动多，回来点一次「重建索引」即可（只重建变化过的笔记）
- 旧的、已经删掉的笔记的向量会在重建时清掉

### 这些功能

| 功能 | 在哪 | 效果 |
| --- | --- | --- |
| **AI 生成摘要** | 编辑器「发布设置」里的按钮 | 生成一句话摘要填进摘要框，你可以改了再保存 |
| **AI 推荐标签** | 同上 | 推荐 3–5 个标签，会优先复用你已经用过的标签 |
| **AI 起标题** | 同上 | 根据正文起一个标题填进标题框，不满意可以直接改 |
| **AI 推荐分类** | 同上 | 从你的分类里挑一个最合适的填进分类框，没有合适的不会硬塞 |
| **笔记助手** | 导航「更多 → 笔记助手」 | 用一句话让它替你动手：搜笔记（关键词/标签/分类/近 N 天）、读写与总结、标签增删、分类、归档 / 置顶 / 加星 / 公开博客 / 回收站恢复、库统计。它能分多步操作，执行过程逐步实时汇报；勾上「只读模式」就只查不改；写错可从版本历史回退 |
| **夜间整理** | 自动运行 | 每天自动给没分类的笔记补分类、把「收件箱」标签换成「已归档」。`INKNOTE_TIDY=0` 可关 |
| **问笔记** | 顶部导航「问笔记」 | 检索 + 回答，支持**连续追问**、**流式输出**（一个字一个字蹦出来）、**一键存成笔记** |

### 问笔记怎么用（带多跳检索）

资料不够时，AI 会**自动追加更精确的检索**再回答（最多 3 轮检索合并去重），复杂问题明显更准；
来源列表会包含每一轮用到的笔记。


- 输入框里打字，`Enter` 发送、`Shift+Enter` 换行
- 回答会**边生成边显示**，不用干等
- 下面列出**引用到的笔记**，点开可以核对原文
- 回答下面有「**存成笔记**」按钮：把这一轮问答存成一篇笔记（正文含回答 + 参考来源的双链）
- 右边栏是「**最近提问**」，历史都在，可以接着问、也可以删掉
- 多轮追问时，前面几轮问答会一起带给模型，所以可以问「那它和 XX 比呢？」这种承接性问题

### 笔记助手怎么用

导航「更多 → **笔记助手**」，用一句话说清要做什么，它会自己搜笔记、动手改、然后向你汇报每一步。

- **执行过程实时可见**：它每完成一步（搜到了什么、改了哪篇），页面上立刻多一行
- **只读模式**：勾上之后它只能查不能改——想先看看它打算干什么，就先开着这个勾
- **能做的不止加标签**：按关键词/标签/分类/最近 N 天搜笔记，读写笔记正文与摘要，
  加标签/删标签/换分类，归档、置顶、加星、公开到博客，从回收站恢复，做笔记库统计
- **能批量干活**：「给提到 Docker 的笔记都加上『部署』标签」这类要求它一次就能办完一批
  （最多 50 篇），批量的结果会如实告诉你改了几篇、哪几篇本来就是这个状态、哪几个 id 找不到
- **追加不覆盖**：「在那篇末尾再补一段」它会**在原文后面接上**，不会把整篇重写一遍
- **能看回收站**：可以列出回收站里有什么、还能恢复多少天，再决定恢复哪一篇
- **危险操作要你点头**：让它删笔记时，它**不会直接删** —— 页面上会弹出一张**红色确认卡片**，
  写着「确认把《xxx》移入回收站？软删除，30 天内可在回收站恢复」，你点「确认执行」才真的动手，
  点「取消」就什么都不发生。卡片 10 分钟内有效，过时自动作废
- **两种保险模式**：勾「**只读**」它只能查不能改；选「**干跑**」它只做规划、
  最后给你一份「本来打算做什么」的清单，先审后放
- **会话记忆**：最近 5 轮任务它都记得；还会自动带上「上一次任务动过哪几篇笔记」，
  所以换浏览器、隔一天再说「继续」「刚才那篇再加个标签」也接得上。不想让它记了就点「清空记忆」
- **长笔记**：一次基本就把整篇读给它（默认 8 千字，超长才续读），
  不会再出现「只看到前半段、于是反复读同一篇」的情况
- **中断也不交白卷**：模型超时或报错时，它会把它已经做完的步骤列出来，
  并提示去设置页调大「AI 超时」，而不是回一句空话
- **安全兜底**：每次修改都存版本历史，不满意进笔记的「历史版本」一键回退；
  删除是「移入回收站」（软删除，30 天内可恢复），而且只在你说要删时才用
- **历史任务**：最近 20 次执行都记录在助手页下方（换浏览器也不丢），谁在什么时候动过什么一目了然

> 指令小技巧：说清楚对象和动作效果最好——「给提到 Docker 的笔记都加上『部署』标签」
> 比「整理一下标签」更容易一次做对。

### 那年今日

打开「我的笔记」，如果**往年今天的这一天**你写过笔记，顶部会出现一张「那年今日」
卡片，点标题就能回到那一篇。没有就安静地不出现。

### 归档

写完了、但不想在默认列表里看到的笔记，在详情页点「**归档**」——它会离开默认列表，
但**没有删除**：搜索还能找到，导航「更多 → 我的笔记 → 已归档」筛选里随时能看、
能一键取消归档。归档不算删除，也不进回收站。

### 模板：变量与默认模板

模板不只是复制粘贴，还支持**变量**——套用模板的那一刻自动替换成实际值：

| 变量 | 替换成 |
| --- | --- |
| `{{date}}` | 今天日期，如 2026-09-13 |
| `{{time}}` | 当前时间，如 14:30 |
| `{{datetime}}` | 日期 + 时间 |
| `{{year}}` / `{{month}}` | 年份 / 月份 |
| `{{weekday}}` | 星期几，如 星期日 |

比如模板正文写 `# {{date}} 站会记录`，每天用它建笔记，标题日期永远是当天。

另外可以把一个模板**设为默认**（模板卡片上的「设为默认」按钮）：之后点「新建笔记」
不带模板参数时，会自动套用默认模板；卡片上再点一次「取消默认」即可解除。
编辑一篇笔记时，「发布设置」里还有「**存为模板**」按钮——把现在的内容一键变成模板。

#### 让 AI 帮你写模板

编辑器上方有一个输入框（提示「描述你想要的模板，比如：每周复盘 / 读书笔记」）加一个「**AI 生成**」按钮：

1. 写一句你想要什么样的模板，比如「健身打卡记录」
2. 点「AI 生成」→ 它会自动填好**模板名、一句话说明和 Markdown 正文**
3. **它只填进编辑器，不会直接保存** —— 你看一眼、改两句，再点「创建模板 / 保存」

生成时会把可用的变量清单一起告诉 AI，所以出来的模板常常直接就用上了 `{{date}}` 这类变量。
（需要先在设置页配好 AI 服务；没配会提示你。生成的调用也会计入用量统计。）

#### 调整模板顺序：拖

模板卡片**左上角有个 `⠿` 小手柄**：

- **按住它往上/往下拖**，其它卡片会平滑让位，松手即落位、自动保存
- 键盘也能排：`Tab` 聚焦到手柄后按 **`Alt + ↑` / `Alt + ↓`** 移动一位
- 只有按住**手柄**才能拖（卡片其它地方拖不动，免得误操作碰到里面的按钮）
- 保存失败会提示并把顺序**退回去**，不会界面和实际不一致
- 触屏上暂时不能手指拖（浏览器拖拽 API 的限制），手机上请用键盘方式

#### 收起整个模板列表

「**全部模板（N）**」这个标题本身就是个开关：点它能把左侧整个模板列表**平滑收起**，
再点展开。收起状态会被记住，下次进来还是你上次的样子。模板很多、只想专心写的时候挺好用。

### 统计页的写作热力

统计页顶部有一张**年度热力图**：每个小格子是一天，颜色越深那天写得越多；
鼠标悬停能看到具体日期和篇数。图例在右下角。

### 专注模式与导出

编辑器右上角「**专注**」按钮：隐去导航、工具栏和发布设置，正文居中放大，
Esc 或再点一次退出，下次打开自动恢复。笔记详情页的「**导出网页**」能把这篇笔记
导出成一个**自包含的 HTML 文件**——排版完整、自带亮暗主题，发给谁都能直接打开。

### 配色预设

导航「更多」菜单底部可以切换整套配色，共六套：**墨迹**（默认暖纸色）、**竹青**（青绿）、
**墨蓝**（蓝灰）、**霞紫**（藕荷）、**琥珀**（暖黄）、**石墨**（冷灰极简）。
配色和亮暗模式互相独立、两两组合都协调，选择会记住。

### 夜间整理

开启 AI 后每天自动做两件小事：给没分类的笔记**补一个分类**（优先复用你已有的分类）、
把打了「**收件箱**」标签的笔记换成「**已归档**」。每次最多处理 50 篇，改动都存版本历史。
设置页「AI 服务」分组里有「**夜间整理**」折叠块，能看到上次跑了什么、整理了多少篇。
不想要它就在 `.env` 里加 `INKNOTE_TIDY=0`。

### 关于隐私

- API Key 只存在你自己电脑的数据库里（**页面不回显完整密钥**，只显示 `sk-……1234` 这样的掩码）
- AI 调用是**服务器端**发起的，网页里看不到你的 Key
- 但**被检索到的那几篇笔记内容会发送给 AI 服务商**。介意的话：打开「发送前脱敏」+「只允许本机模型」，
  或者干脆别配 AI（不配时「问笔记」只做本地检索、完全不联网）

### 关于准确性

AI 只依据检索出来的笔记作答，检索不到就会说「没找到相关内容」；但它仍可能理解错，
重要结论请点开下面列的原文核对。

### 想用文件配置也行

不想用网页的话，`.env` 里这几个键同样有效（网页没保存过配置时生效）：

```ini
INKNOTE_AI_BASE_URL=https://api.deepseek.com/v1
INKNOTE_AI_API_KEY=sk-你的key
INKNOTE_AI_MODEL=deepseek-chat
INKNOTE_AI_EMBED_MODEL=text-embedding-3-small
# INKNOTE_AI_TIMEOUT=45
```

## 十四、批量操作、站点设置、图片管理

> **操作流**：想把同一批笔记一次做完几件事（比如「加星标 → 设为分类 → 归档」），
> 在批量栏选好第一个动作后点「＋ 加入操作流」，再选下一个动作继续加入（最多 5 步，
> 顺序可调整、可单步移除），最后点「执行操作流」——一次提交、分步汇报。
> 不用操作流时，原来的单动作「执行」完全照旧。

### 批量操作（笔记一多就用得上）

笔记列表页每张卡左上角有个**复选框**：

1. 勾选要处理的几篇（顶部的「全选」可以一次勾上当前这一页）
2. 在顶部那条操作栏里选一个动作：**添加标签 / 移除标签 / 设为公开 / 取消公开 / 置顶 / 取消置顶 / 加星标 / 取消星标 / 移入回收站**
3. 点「执行」→ 回来时会告诉你「已处理 N 篇、跳过 M 篇」（跳过的是查不到的，比如已经被彻底删掉的）

选「添加标签」或「移除标签」时，旁边才会出现标签输入框。**没有 JS 也能用**，只是少了实时的选中计数。

### 站点设置（不用再改 .env）

页脚「设置」页现在有「**站点信息**」一块，能直接改：

| 字段 | 作用 |
| --- | --- |
| 站名 / 副标题 | 浏览器标签页标题、页面顶部和页脚的站名 |
| 站点描述 | 博客首页的介绍文字、SEO description |
| 作者 | RSS / Atom 订阅里的作者名 |
| 站点地址 | 生成 RSS / sitemap 用的完整域名 |
| 每页条数 | 列表页、博客页每页显示多少篇（5~200） |
| 回收站保留天数 | 超过天数的会在下次启动时被清掉（0 = 永不清理，0~3650） |

**保存后立刻生效**，不用重启。优先级还是：**设置页 → `.env` → 默认值**，
每个字段旁边的小标签会告诉你当前值是哪来的。想全部退回 `.env`，点「清除本页配置」。

> 注意：登录密码也可以直接在这个设置页改（见下面「在页面里改密码」）；`.env` 里的
> `INKNOTE_PASSWORD` / `INKNOTE_PASSWORD_HASH` 是页面没配过时的兜底，不用再去改文件。

### 图片管理

页脚「**图片**」页（`/images`）列出你上传过的所有图片：

- 每张图显示 **尺寸 / 上传时间 / 被哪几篇笔记引用**（点标题能跳过去）
- **没有被任何笔记引用的会标成「孤立」**，顶部给出一共几张、占多少空间
- 「**清理全部孤立图片**」一键删掉；确实需要删被引用的图时，要先确认一次
- 删除**只允许操作 `data/uploads` 里的文件**，路径里带 `../` 这类会被直接拒绝

> 删笔记**不会**顺手删图（怕你别的笔记还在用），所以偶尔来这页清一下就行。

## 十五、自动备份、改密码、批量全选

### 自动备份（重要，建议看一眼）

以前只有「手动导出」；现在**启动时会自动备份一次**，之后**每小时**检查一次，超过 24 小时就再备一份，
滚动保留最近 7 份自动备份。备份放在 `data/backups/`，是标准的 SQLite 文件，**可以直接拷到别处**。

页脚「**备份与恢复**」页里能看到备份列表：

| 操作 | 说明 |
| --- | --- |
| **立即备份** | 手动存一份（手动备份**不会**被滚动清理掉） |
| **下载** | 下载到本地存起来（推荐偶尔往网盘/U 盘放一份） |
| **回滚** | 用某份备份**覆盖当前数据**；回滚前会自动把「现在的数据」另存为 `before-restore` 那份，所以误点也能救回来 |
| **删除** | 删掉某份备份 |

> 回滚是「原地写回」而不是替换文件，所以**不用重启服务**；但回滚会覆盖从那之后的所有改动，操作前想清楚。
> 另外：备份是**整库**快照，导出 zip 是**单篇 Markdown** —— 前者用来救灾难，后者用来搬家和存档。

### 导入（搬家进来）

「备份与恢复」页的**导入 / 恢复**支持三种格式：`.zip`（整包，含图片）、`.json`（导出 JSON）、`.md`（单篇或多篇 Markdown，
可以一次勾选多个文件）。Markdown 里认 YAML front matter（`title` / `tags` / `category` / `summary` / `slug` / `public` / `status`），
没有就按正文 `# 标题` 或文件名兜底。按 slug（没有就按标题）幂等合并：**匹配到就更新，匹配不到就新建**，绝不删你已有的笔记；
拿不准就先勾「先预览」。

### 在页面里改密码

设置页多了一个「**账号**」区块：填**旧密码 + 新密码 + 确认新密码**即可，不用再编辑 `.env`。

- 新密码至少 8 位；两次输入要一致；不能和旧密码相同
- 旧密码连错 5 次会锁 5 分钟（防止有人在你电脑前瞎试）
- 改完**已登录的设备不会被踢下线**（会话是签名 cookie，和密码无关）
- 忘了密码：删掉页面配置（设置页里有「清除本页配置」）或改 `.env` 里的 `INKNOTE_PASSWORD`
- 优先级：**页面改的 → `.env` 的 `INKNOTE_PASSWORD_HASH` → `.env` 的 `INKNOTE_PASSWORD`**

### 批量操作：选中「全部筛选结果」

先筛选（比如按标签），再勾上操作条上的「**选中全部筛选结果（N 篇）**」，
就会对**符合当前条件的全部笔记**生效，而不是只有当前页。

- 一次最多 500 篇（超出的不会处理，flash 里会说清楚）
- **一个筛选条件都没有时，必须再勾一次确认**才会执行 —— 免得手一抖把整个库改了
- 没勾选时行为不变（只处理你手动勾的那些）

### 图片库的两个改进

- **分页**：每页 24 张，筛选（只看孤立图片）和分页可以一起用
- **复制 Markdown 链接**：每张图一个按钮，一键复制 `![](图片地址)`，直接粘进笔记
- 被引用的图会列出**引用了它的笔记**（默认前 3 篇，可展开全部）

### 删笔记前会提示引用了多少图

卡片上的删除确认、详情页的删除按钮，都会告诉你「正文引用了 N 张图片」，详情页另外有一行
小字链到图片页 —— 删完记得去清理，不然图会一直留在磁盘上。

### 改完代码想自检？一条命令

```powershell
.venv\Scripts\python.exe scripts\check.py           # 语法 + 样式 + 全量测试 + 冒烟 + 数据库完整性 + 环境（约 22 秒）
.venv\Scripts\python.exe scripts\check.py --quick   # 跳过大部分测试，几秒钟出结果
```

想单独跑测试（并行，约 24 秒）：

```powershell
.venv\Scripts\python.exe -m pytest tests -n 8 --dist loadfile -q
```

---

## 十六、日志、导出图片、更多细节

### 日志现在会写文件

以前日志只在控制台，关掉窗口就没了。现在会自动写到 **`data/logs/inknote.log`**，**按天切分、保留 14 天**。

- 想看服务出了什么事（比如 AI 调用失败、备份失败、搜索降级），翻这个文件就行
- 想调级别/保留天数，在 `.env` 里加：`INKNOTE_LOG_LEVEL=DEBUG`、`INKNOTE_LOG_DAYS=30`
- 写不进去（比如磁盘满了）不会影响服务，只会在控制台留一句警告

### 导出的 zip 现在带图片了

以前 `/export/zip` 只打笔记的 `.md` 和 `notes.json`，**图片没跟过去** —— 换台机器导入后图全是死链。现在：

- zip 里多一个 `media/` 目录（保持原来的路径结构），还有一个 `BACKUP-README.txt` 说明
- 导回来时图片会**还原到 `data/uploads/` 对应位置**；已存在且内容相同就跳过，不同则并存（加 `-1` 后缀，不覆盖你的）
- 导入结果会报「图片新增 N 张、跳过 M 张」
- 只导出**笔记正文里真的引用到**的图片；纯粹没用的孤立图不会进包

### 向量索引会自动跟进

配了「向量模型」之后，以前改完笔记得记得回设置页点「重建索引」，忘了就是旧内容在参与语义检索。
现在**后台每 30 分钟自动检查一次**，只重算改动过的那几篇（增量），不重复烧钱。服务刚启动时也会先查一次。

### 备份：多了风险提示和「脱敏备份」

备份文件（`.db`）里含 **AI 密钥等敏感信息**，直接发人不合适。所以：

- 备份页顶部有醒目提示
- 多了一个「**脱敏备份（可安全分享）**」按钮：快照里的 `ai.api_key`、密码哈希会被清空，**其余数据完整**，回滚也能用

> **没有做加密**。Python 标准库没有 AES 这类安全的对称加密，自己手搓必然不可靠 —— 真要保密请用系统的加密盘/加密压缩包（比如 7-Zip 带密码）。

### 回收站会告诉你还剩几天

`/trash` 顶部现在写明「保留 30 天，会在下次启动服务时清理」，每条笔记上还有倒计时：
`还有 12 天` / `明天清理` / `已过期，下次启动时清理`（`trash_days=0` 时显示「不自动清理」）。

### 批量勾选现在跨页保持

列表页勾了几篇，翻到第二页、换个筛选，**回来还是勾着的**，操作条会显示「已选 N 项（含其它页 M 项）」，
提交时把其它页选中的也一起带上。旁边有「清空选择」。提交成功后会自动清空，不会下次打开还勾着一堆。

### 图片按内容去重

- **上传时**：同一张图传第二次，不会再存一份，直接复用已有文件（响应里 `deduped: true`）
- **图片页**：重复的图会有「内容重复」标记，顶部显示「重复 N 组，可省 X」
- 「清理多余副本」只删**没有任何笔记引用**的那几份；两份都被引用的，只提示、不自动删

### 手机上的编辑器工具栏收起来了

窄屏（≤640px）只留最常用的几个（加粗 / 斜体 / 代码 / 链接 / 图片），其余 12 个收进「**更多 ▾**」，点开全部展开、再点收起。
桌面端完全不变（还是全部平铺）。纯 CSS 实现，键盘也能操作。

---

## 十七、关系图谱

导航「更多 → **关系图谱**」（地址 `/graph`；也可以在命令面板里输 `txp` 跳过去）。

它把你笔记之间的 `[[双链]]` 画成一张网：**圆点 = 一篇笔记，连线 = 一条 `[[引用]]`，箭头表示「谁引用了谁」**。
想看整体结构、找出哪几篇是枢纽、哪些还没接进网里，用这张图最直观。三件事在图上直接看得出来：

- **谁最"中心"**——圆点越大，连接数越多
- **有哪几片**——颜色是自动分出来的聚类（关系密集的一坨同色），下面配图例
- **谁被连回来了**——互相引用的两篇画成两条分开的弧线，不重叠

### 怎么看

| 操作 | 效果 |
| --- | --- |
| **鼠标移到圆点上** | 高亮和它直接相连的笔记，其余变淡；浮出一张小卡片，写着连接数（入/出）、标签、分类 |
| **点一下圆点** | 打开那篇笔记 |
| **按住圆点拖动** | 挪到顺手的位置，**松手后就留在那儿**（邻居会轻轻让开，不会把它拽回原位） |
| **滚轮 / 触屏双指捏合** | 缩放（鼠标在哪就以哪为中心） |
| **拖空白处** | 平移整张图（手机上是**双指拖动**平移；单指留给页面滚动） |
| **方向键** | 焦点在图上时 `↑↓←→` 在圆点间移动、`Enter` 打开；`+` `-` `0` 缩放与适应 |
| **搜索框** | 输标题片段即时高亮命中项（其余变淡），`Enter` 选中第一个并居中。**搜索时连未链接的孤立笔记也会带出来** |
| **「只看有链接的笔记」** | 默认勾选——先把谁都没连的孤立笔记收起来，图更干净；取消勾选可以一眼看出"哪些还没接进网里" |
| **「按标签筛选」** | 只留带某个标签的笔记（连带它们的连线） |
| **着色方式** | `按聚类着色`（默认，看图里有几片）/ `按标签着色`（同色 = 同一类标签，和标签药丸同色）/ `按分类着色`（同色 = 同一分类）/ `单色` |
| **「路径」** | 点两篇笔记，看**它们之间是怎么连上的**（见下） |
| **适应视图 / 重新布局** | 前者把全部圆点收回视野；后者重新撒开重排（换回随机布局，用于卡住时重来） |
| **导出 PNG / 导出 SVG** | 把整张图存成图片（见下） |

### 两篇笔记是怎么连上的（「路径」）

点工具栏的「**路径**」，再点两篇笔记，图就会**把最短的那条链路点出来**：起点是品牌色实心圆、
终点是强调色实心圆、中间的每一跳都描边加粗，连线上有流动的虚线，其余全部压暗。
下面一行会写清楚它是怎么绕过去的，例如：

> 最短路径 2 步：《周报 09-08》 → 《项目 A 复盘》 → 《下季度计划》

- 键盘也行：`↑↓←→` 选到某一篇、`Enter` 记下来 —— 两次就是一个起点一个终点
- 再点第三次会**重新开始选**；点「清除路径」或按 `Esc` 退出路径模式
- 「路径模式」里点圆点是**选端点**（不再打开笔记）；想打开笔记先按 `Esc` 退出
- 两篇之间没有通路时会直说「没有连通的路径」，并提醒你可能是筛选挡住了（见下）

> 用法举例：想不起某篇笔记为什么相关、或者整理旧笔记时想看"这条线索是从哪儿牵过来的"，
> 比一层层点进去快得多。

### 在图上直接建立联系

选好路径的两端之后，下面会出现「**建立联系**」两个按钮（`A → B` 和 `B → A`）：

- 点一下 = **在 A 的正文末尾加上指向 B 的链接**（和手写 `[[B]]` 完全一样，也会留版本快照，可回滚）
- 两个方向各点一次就是**互相链接**（图上会立刻出现两条分开的弧线，不用刷新）
- 已经连过会提示「这篇里已经连到《B》了」，不会重复追加

> 图上一次性把两篇连起来，比"打开 A、翻到末尾、手打标题"顺手。只想看关系不想动笔，就按 `Esc` 退出。

### 把图存成图片

工具栏右侧「**导出 PNG**」/「**导出 SVG**」：

- **PNG**——两倍分辨率位图，适合发聊天、贴文档；**SVG**——矢量图，放大不糊，适合打印或再编辑
- 导出的是**整张图**（不管你现在缩放到哪儿），自动按内容裁边留白，并带上当前主题的背景色
- 文件名形如 `inknote-graph-20260915-2140.png`
- 图上的颜色和大小都是导出那一刻的样子（所以先调好着色方式再导）

### 布局松紧

工具栏下方有「**布局松紧**」，三个滑杆边拖边生效：

| 滑杆 | 作用 |
| --- | --- |
| **斥力** | 越大圆点之间越互相推开（图更舒展、更容易看清连线） |
| **连线长度** | 相连两篇的理想距离（调小会让关系紧密的抱得更紧） |
| **向心** | 越大整张图越往中心收 |

调完**记在这台浏览器上**，下次打开还是你习惯的松紧；点「恢复默认」还原。
想彻底重排一遍（换回随机初始位置）还是用「重新布局」。

### 位置会被记住

图的布局是自动算出来的（互相排斥、被链接的互相吸引），两三秒稳定下来就**停止计算**（放着不动不占 CPU）。
你的调整和最终布局会**记在浏览器里**：下次打开、或从别处点回来，图基本还是你熟悉的样子，
不会再每次都重新乱排一遍。想推倒重来就点「重新布局」。

### 从某篇笔记看它的邻居：局部关系图

打开任意一篇有链接的笔记，**右侧栏有一张「局部关系图」**——只画这篇和它的邻居：

- **中心那篇是品牌色**、比邻居大一圈
- **点邻居直接跳过去**，看图换乘很方便（比一层层点回去快）
- 默认 1 跳；下方「**看 2 跳**」能把邻居的邻居也带进来，「只看 1 跳」切回来
- 右上角「**全图 →**」跳到全局图谱，并自动选中、居中这篇

> 这张图比全局图更常用：全局图看的是"整体长什么样"，局部图回答的是"这篇周围都有谁"。

### 几点说明

- **聚类是怎么来的**：后端按链接密度自动分组（Louvain 社区检测），不是按标签分的。
  想看标签维度的分片，把着色方式切成「按标签着色」。
- **在图上建链接**：先用「路径」选出两篇，下面就会出现「建立联系」按钮（见上）；
  另外第 6 节的 [[ 输入补全、笔记页的「建立联系」表单、局部图下方的直达入口，都是同一件事。
- **悬空链接不算数**：正文里写了 `[[标题]]` 但那篇笔记还不存在（显示虚线待创建）时，图谱里不画它；
  等笔记建好会自动接上。
- **笔记多的时候**：圆点超过 55 个时标题先隐起来（悬停、选中、搜索命中的会显示），避免糊成一片；
  节点数上千时后端会换用近似算法（Barnes–Hut）保持流畅。
- **没有 JS 也能用**：脚本没加载时，全局图谱退回成一份「最近有链接的笔记」列表、局部图退回成邻居列表，不会白屏。
- 笔记越多、`[[双链]]` 写得越勤，这张图越有用。

---

## 十八、界面动效与首次引导

### 第一次打开会有个四步引导

第一次进站点时会看到一圈高亮加一个小气泡，带着你认路：

1. 「**写笔记**」按钮
2. 「**快速搜索**」——顺带告诉你 `Ctrl+K` 能开命令面板
3. 笔记列表页的**标签筛选**
4. 右上角的**深色模式月亮**

节奏自己定：「下一步 / 跳过」，按 `Esc` 也能退出，方向键也能切。
看过一次就**永不再出现**（记在浏览器本地）。它只打扰你这一次。

### 悬停提示全站统一

整个站点的鼠标悬停提示（图标按钮、图表柱子、图谱节点……）都是同一套**自绘浮层**：

- **停住约 0.16 秒才弹**——鼠标扫过去不会一路弹气泡
- **移开立刻收起**，不会留一个飘着的框
- 该提快捷键的按钮（`Ctrl+K` 之类）提示里会直接写出来
- 用键盘操作（Tab、方向键）时**不延迟**，立刻显示

> 早先用的是浏览器自带的小黑气泡：慢、样式不统一，偶尔还会和自绘的重复弹出来。现在全站只剩一种了。

### 一堆小动效

都是为了让操作有反馈。系统开了「**减少动态效果**」时，它们会全部自动关掉。

| 位置 | 动效 |
| --- | --- |
| 右上角月亮 | 鼠标移上去**转一下**；点它切换主题时月亮**整圈翻转**，整页颜色柔和过渡（不再生硬跳变） |
| 「最近提问」/「历史任务」/「全部模板」/「布局松紧」等折叠块 | 展开收起是**平滑的高度动画**，箭头同步旋转 |
| 模板卡片 | 打开页面时**逐张上浮淡入** |
| 待办清单 | 勾选时**删除线从左划到右**、文字变淡 |
| 编辑器 | 自动保存成功时状态**轻弹一下**变绿；字数统计回包时**轻跳** |
| 问笔记发送 | 发送后**纸飞机飞出去**再归位 |
| 删除笔记 | 确认后卡片**缩扁淡出**再跳转 |
| 图标按钮 | 垃圾桶**摇晃**、放大镜**凑近**、加号**转 90°**、铅笔**起笔** |
| 切换页面 | 主内容**淡入**（很轻，不晃眼） |

### 展开东西时页面不会左右晃

以前展开「历史任务」这类内容，页面会左右动一下——那是滚动条出现/消失把内容挤了一下。
现在滚动条位置**恒定预留**，展开再多东西页面也不会横移。

---

---

*这份说明对应的是 2026-09 中旬的版本。功能都实测过；如果你发现哪里写的和实际不一样，按实际为准。*
