# 墨痕 InkNote · 使用说明

> 自己用的网页笔记工具：**用 Markdown 写笔记，勾一下「公开」就变成博客文章**。
> 数据全部存在你自己电脑的 `data\` 文件夹里，不联网、不上传、不需要注册账号。
>
> 这份文档在网站上也能看：登录后点页脚的「**使用说明**」（地址 `/manual`）。

---

## 目录

1. [认识墨痕](#认识墨痕)
2. [启动与登录](#启动与登录)
3. [页面地图](#页面地图)
4. [写笔记](#写笔记)
5. [Markdown 速查](#markdown-速查)
6. [标签与搜索](#标签与搜索)
7. [双链与关系图谱](#双链与关系图谱)
8. [发布成博客](#发布成博客)
9. [历史版本、回收站与归档](#历史版本回收站与归档)
10. [模板](#模板)
11. [批量操作](#批量操作)
12. [备份与搬家](#备份与搬家)
13. [图片管理](#图片管理)
14. [AI 功能（可选）](#ai-功能可选)
15. [设置页一览](#设置页一览)
16. [快捷键与命令面板](#快捷键与命令面板)
17. [外观与多设备](#外观与多设备)
18. [常见问题](#常见问题)

---

## 认识墨痕

一句话：**写**（Markdown 编辑器，顺手有 AI 帮忙）、**找**（全文搜索、标签、双链、关系图谱）、
**发**（勾一下公开，就是你的博客）。

所有东西都在这个文件夹里，**拷走它 = 拷走全部数据**：

```
C:\repo\inknote\data\
├── inknote.db       ← 全部笔记、标签、历史版本、模板、AI 配置（一个 SQLite 数据库）
├── uploads\         ← 你上传的图片，按 年\月 分目录
├── backups\         ← 自动备份与手动备份（整库快照）
├── logs\            ← 运行日志，按天切分、保留 14 天
└── .secret          ← 登录会话密钥（别删，也别给别人）
```

**什么时候会联网**：只有你主动配置并使用 AI 功能时，被检索到的那几篇笔记内容才会发给 AI 服务商
（见第 14 节的隐私说明）。除此之外一切都在本机。

---

## 启动与登录

**日常用法**：双击桌面上的「墨痕笔记」图标，就这一下。

接下来会自动发生三件事：

1. 弹出一个黑色窗口（这是网站程序本体，**用的时候别关它**）
2. 等 3～5 秒，浏览器自动打开 <http://127.0.0.1:8000/>
3. 点右上角「登录」，输入密码，回车（初始密码是 `inknote`，**登录后请到设置页改掉**）

**用完想关掉**：把那个黑色窗口关掉就行。服务已经在跑时又双击一次图标也没关系，
它不会报错，会直接帮你打开浏览器。

文件夹里还有两个启动脚本，按需用：

| 脚本 | 干什么 |
| --- | --- |
| `start-local.bat` | 日常本地使用：启动服务 + 自动打开浏览器 |
| `start-inknote.bat` | 本地 + **公网隧道**：多开一个窗口，里面会打印一个 `https://…trycloudflare.com` 网址，把它发到手机或别的电脑就能访问（网址每次启动会变；不用了把两个窗口都关掉） |

---

## 页面地图

### 顶部导航（登录后）

左边三个是常驻入口：

| 菜单 | 干什么 | 地址 |
| --- | --- | --- |
| **笔记** | 你的全部笔记（含草稿），主战场 | `/notes` |
| **标签** | 所有标签 + 按标签分组的笔记 | `/tags` |
| **博客** | 对外展示的公开页面，**不需要登录** | `/blog` |

其余的收在右边的「**更多 ▾**」里：

| 更多菜单 | 干什么 | 地址 |
| --- | --- | --- |
| **模板** | 管理笔记模板：新建 / AI 生成 / 拖拽排序 / 设为默认 | `/templates` |
| **问笔记** | 在你的笔记里检索，让 AI 作答；可连续追问、一键存成笔记 | `/ask` |
| **笔记助手** | 用一句话让它替你动手改笔记，执行过程实时可见 | `/agent` |
| **今日笔记** | 一键打开今天的日记，没有就自动建好 | `/notes/today` |
| **今日待办** | 把各篇笔记里的待办清单汇总到一页勾选 | `/todos` |
| **写作统计** | 字数、年度热力图、写作节奏 | `/stats` |
| **关系图谱** | 把 `[[双链]]` 画成一张网，看笔记之间的脉络 | `/graph` |
| **配色** | 切换整套配色（见第 17 节） | — |
| **回收站** | 删掉的笔记先放这里，30 天内可恢复 | `/trash` |

页面**最底部**还有几个入口：

| 页脚入口 | 干什么 | 地址 |
| --- | --- | --- |
| **使用说明** | 就是这份文档 | `/manual` |
| **设置** | 站点信息、外观、AI 配置、改密码（见第 15 节） | `/settings` |
| **备份与恢复** | 导出 / 导入笔记、整库备份与回滚、远端备份 | `/backup` |
| **图片** | 管理上传过的图片（见第 13 节） | `/images` |
| **导出全部笔记** | 打包下载全部 Markdown | `/export/zip` |

### 笔记列表页

- 顶部四个数字：**累计字数 / 连续写作天数 / 今日更新 / 标签数**（小激励面板）
- 搜索框：搜**标题 + 正文 + 标签**，多个关键词用空格分开（要同时命中）
- 筛选与排序：状态下拉、排序下拉、分类下拉，再加一排胶囊（`全部` `★ 星标` `置顶` `已公开`）和**标签胶囊**
- 每张卡片上的小按钮：✏️ 编辑、📌 置顶、★ 标星、👁 公开、🗑 删除——**点完立即生效**，页面不整页刷新；标星点亮时有一记金色小弹跳
- **置顶的笔记可以拖动排序**：抓住置顶卡片左上角的 `⠿` 拖到新位置（或聚焦手柄后按 `Alt+↑` / `Alt+↓`）；只影响置顶笔记之间的先后
- 点删除会先问一句「正文引用了 N 张图片」，确认后卡片缩扁淡出再跳转
- 如果**往年今天的这一天**你写过笔记，列表顶部会出现「**那年今日**」卡片，点标题就能回到那一篇；没有就安静地不出现

### 笔记详情页（点标题进去）

左边是正文，右边一栏（手机上会跑到下面）依次是：

- **目录**：正文里的标题自动生成，滚动时高亮当前章节
- **你可能还想看**：自动推荐的相关笔记（会说明推荐理由；配了向量模型时按语义推荐）
- **反向链接 / 指向**：谁引用了这篇、这篇引用了谁；目标还不存在时显示虚线 + 「创建 →」
- **局部关系图**：这篇笔记的邻居（默认 1 跳，可切「看 2 跳」），点圆点直接跳过去
- **最近历史**：最近几个版本快照

右上角按钮：**编辑**、**公开 / 取消公开**、**置顶**、**标星**、**历史**、**导出单篇**、**导出网页**、**在博客中查看**、**归档**、**移入回收站**。

> 页面顶部有一条细线是**阅读进度条**（长文章才有，读完淡出）。博客文章页、使用说明页也有同一条。

---

## 写笔记

编辑器从上到下是这样：

```
┌────────────────────────────────────────────┐
│  标题输入框        编辑 | 分栏 | 预览   保存状态 │
├────────────────────────────────────────────┤
│  B  I  S | H1 H2 H3 | 列表 待办 引用 | 代码 …  │  ← 工具条
├──────────────────────┬─────────────────────┤
│   左边：写 Markdown    │   右边：实时预览       │
├──────────────────────┴─────────────────────┤
│  字数 · 阅读时长 · 第几行      支持拖拽/粘贴图片  │
├────────────────────────────────────────────┤
│  ▸ 发布设置（标签、分类、摘要、slug、公开开关…）  │
├────────────────────────────────────────────┤
│            存为草稿   保存   保存并查看         │
└────────────────────────────────────────────┘
```

**三个视图**：`编辑` 只看写作区、`分栏` 左写右看（宽屏推荐）、`预览` 只看效果。手机上默认单栏，用顶部标签切换。

**自动保存**：**已经在编辑的笔记**停笔 2.5 秒会自动存一次，右上角显示「保存中… / 已保存」。
**新建的笔记不会自动保存**（还没有编号），第一次一定要点「保存」。

**发布设置**里每个字段：

| 字段 | 说明 |
| --- | --- |
| 标签 | 逗号或空格分隔；正文里写的 `#标签` 也会自动收集进来 |
| 分类 | 比如「技术 / 读书 / 生活」，用来在列表页筛选 |
| 摘要 | 留空就自动截取正文开头；填了就一直用你填的 |
| slug | 网址路径，如 `python-asyncio`；留空按标题自动生成，中文标题也能当 slug |
| meta 描述 | 给搜索引擎看的，留空就用摘要 |
| 公开到博客 | 勾上就出现在博客里 |
| 置顶 / 星标 | 置顶排最前，星标方便快速筛选 |

底部三个按钮：**存为草稿**（不进博客，还能继续改）、**保存**（留在编辑器继续写）、**保存并查看**（跳到详情页看效果）。

**插图**三种方式都行，图片存到本地 `data\uploads\`：拖进编辑区、`Ctrl+V` 粘贴截图、点工具条「图片」按钮选文件。
支持 `png / jpg / gif / webp / bmp / avif`，单张最大 8MB，多张会依次上传。

**专注模式**：编辑器右上角「专注」按钮，隐去导航与设置、正文居中放大，`Esc` 退出。

**导出**：详情页「导出单篇」下载 `.md`；「导出网页」生成一个**自包含的 HTML 文件**（排版完整、自带亮暗主题），发给别人即开即读。

---

## Markdown 速查

> 记住一条就行：**标题的 `#` 后面必须有空格**。`# 标题` 是标题，`#标题` 是普通文字（这样正文里的 `#标签` 才不会被吃成标题）。

| 想要的效果 | 这么写 |
| --- | --- |
| 一级标题 | `# 标题` |
| 二级标题（**推荐**，会进右侧目录） | `## 小标题` |
| 三级标题 | `### 更小的标题` |
| **加粗** / *斜体* / ~~删除线~~ / 高亮 | `**加粗**` / `*斜体*` / `~~删除线~~` / `==高亮==` |
| 行内代码 | `` `代码` `` |
| 代码块（带语法高亮） | 用三个反引号围起来，第一行后面写语言名，比如 python |
| 无序 / 有序列表 | `- 一项` / `1. 一项` |
| **待办清单** | `- [ ] 没做` / `- [x] 做了` —— 阅读视图里点复选框直接打勾并保存（博客公开页只读） |
| 引用 | `> 引用一段话` |
| 分割线 | `---` |
| 链接 | `[文字](https://example.com)` |
| 图片 | `![说明](https://example.com/a.png)` |
| 表格 | 见下面 |
| 脚注 | 正文写 `[^1]`，末尾写 `[^1]: 注释内容` |
| 双链 | `[[另一篇笔记的标题]]` |
| 标签 | `#技术` |
| **数学公式** | `$$E = mc^2$$`（行间）或 `\(a+b\)`（行内）；单个 `$` 不算公式，写价格不用转义 |
| **流程图 / 时序图** | 代码块语言写 `mermaid`，里面写 `graph LR` / `A --> B` |
| **提示块** | `> [!NOTE] 标题`，类型：`NOTE` `TIP` `SUCCESS` `WARNING` `DANGER` `QUOTE`，也认中文（`注意` `提示` `警告` `危险` `引用`）；行尾加 `-` 变可折叠：`> [!TIP]- 点开看` |
| 缩写解释 | 末尾写 `*[HTML]: HyperText Markup Language`，正文里的 HTML 会带上悬停解释 |

提示块长这样（六种类型颜色不同，带 `-` 的默认收起、点标题展开）：

```markdown
> [!NOTE] 默认提示
> 补充说明，语气是中性的。

> [!WARNING] 小心
> 会覆盖已有内容。

> [!TIP]- 点开看折叠内容
> 默认收起，适合放长一点的补充说明。
```

表格写法（第二行的 `---` 是必须的）：

```markdown
| 项目 | 说明 |
| --- | --- |
| 后端 | 本机运行 |
| 数据 | SQLite |
```

代码块写法：第一行三个反引号后面写语言名，最后单独一行三个反引号收尾。下面用四个反引号把例子包起来，好让你看清里面那三个反引号：

````markdown
```python
def hello(name):
    return "hi " + name
```
````

支持 `python` `javascript` `bash` `json` `sql` `go` 等几十种语言名，代码块会按语言着色。

**不支持**在正文里写 HTML 标签——会被当成纯文本显示，这是为了安全。

---

## 标签与搜索

### 标签

两种打标签的方式，可以混用：

1. 编辑器「发布设置 → 标签」里填，逗号或空格分隔：`python, 异步, 踩坑`
2. 直接在正文里写 `#异步`（`#` 后面不要空格；代码块里的不算）

编辑器里还有两个顺手的：输入框下面列出你**最常用的标签**（点一下直接加/去掉），以及边打字边出的**自动补全**。

**标签页**（导航「标签」）：上面是标签云，下面按标签分组列笔记；右上角能搜标签、按名称/使用次数排序。
每个标签药丸有自己的固定颜色（同一个标签在哪儿都是同一个色），后面的小徽标写着用了几次。

**管理标签**（标签页上方，默认收起）：

| 想做的事 | 怎么做 |
| --- | --- |
| 改名 / 合并 | 「管理标签」里选标签、填新名字。新名字填**已存在的标签**就是合并，关系取并集不会重复 |
| 删除 | 展开标签胶囊旁的 ✎ → 删除。只把标签从笔记上摘掉，笔记本身不动 |
| 清理没用的 | 「清理未使用的标签」 |

### 搜索

三个入口随便用：列表页搜索框、命令面板（`Ctrl+K`）、搜索结果页 `/search`（命中词高亮）。

支持轻量语法，可以和普通关键词混用：

| 写法 | 作用 |
| --- | --- |
| `tag:读书` 或 `#读书` | 只搜带这个标签的笔记 |
| `title:周报` | 只匹配标题 |
| `cat:技术` | 只搜这个分类 |
| `"精确短语"` | 引号里整体匹配，不被拆词 |
| `-草稿` | 排除包含这个词的 |
| `after:2026-01` / `before:2026-09` | 按「最近更新」时间过滤（年 / 年-月 / 年-月-日都行） |
| `is:starred` / `is:pinned` / `is:public` / `is:draft` | 只看星标 / 置顶 / 已公开 / 草稿 |

比如 `部署 tag:技术 -草稿` = 分类「技术」里、不含「草稿」、提到「部署」的笔记。
中文不用担心分词：一个字、两个字、长词、词中间的字都能搜到。

搜索是「**所有关键词都要命中**」的逻辑：搜 `异步 循环`，两篇分别含这两个词但同一篇没同时出现，就不算命中。

搜索结果页的结果卡片带完整操作按钮（星标 / 置顶 / 公开 / 编辑 / 删除），还能像列表页那样按标签、分类、状态再筛、按四种方式排序；不筛就保持相关度顺序。

**按意思搜**：结果页有个开关（默认关）。打开后就算用词和笔记里字面不一样也能按语义召回——
前提是设置页配好向量模型并建过索引（见第 14 节）。没配好时自动退回关键词搜索，并标明这次用的是哪种引擎。

---

## 双链与关系图谱

### 双链：把笔记连起来

正文写 `[[另一篇笔记的标题]]`，想显示别的文字就写 `[[标题|显示名]]`。连起来之后：

- 点链接直接跳过去
- 目标笔记的详情页会显示「反向链接」：哪些笔记提到了它
- 目标还不存在时，链接显示成虚线 + 「创建 →」，点一下用它当标题新建
- 「你可能还想看」会把互相引用过的笔记推荐出来

**不用背标题的三条路**：

| 在哪 | 怎么做 |
| --- | --- |
| 编辑器正文里 | 打 `[[` 就地弹出标题候选，打几个字收窄，`↑` `↓` 选、`Enter` 插入，`Esc` 取消 |
| 编辑器工具栏 | 点「`[[ ]]`」按钮，自动打上 `[[` 并打开候选框 |
| 笔记页右栏「指向」 | 底部输入框选一篇 → 「连接」。链接会加在正文末尾 |

**想断开**：「指向」列表里每条右边的 ✕，删掉正文里对应的那处 `[[链接]]`（其它正文不动）。
建立和断开都留版本快照，删错了去「历史」回滚。

**改了标题怎么办**：保存后如果还有笔记引用着旧标题，编辑页顶部会提示——点「一并更新」，
那些笔记里的 `[[旧标题]]` 会自动改写，每篇都留快照；新标题和别的笔记撞名时会先提醒你。

### 关系图谱

导航「更多 → 关系图谱」。它把笔记之间的 `[[双链]]` 画成一张网：
**圆点 = 一篇笔记，连线 = 一条引用，箭头表示谁引用了谁**。圆点越大连接越多；
颜色是自动按连接密度分出来的聚类（一片一片同色）；互相引用的两篇画成两条分开的弧线。

| 操作 | 效果 |
| --- | --- |
| 鼠标移到圆点上 | 高亮它直接相连的笔记，其余变淡；浮出小卡片（连接数、标签、分类） |
| 点一下圆点 | 打开那篇笔记 |
| 按住圆点拖动 | 挪到顺手的位置，松手后留在那儿 |
| 按住 `Shift` 从一个圆点拖到另一个 | 直接建立联系：弹出方向确认，点一下就建好，图上立刻出现连线 |
| 滚轮 / 双指捏合 | 缩放；拖空白处平移（手机单指留给页面滚动） |
| 方向键 + `Enter` | 焦点在图上时逐个选点、打开；`+` `-` `0` 缩放与适应 |
| 搜索框 | 按标题片段即时高亮，`Enter` 选中第一个并居中 |
| 「只看有链接的笔记」 | 默认勾选（图更干净）；取消勾选能看出哪些笔记还没接进网里 |
| 着色方式 | 按聚类（默认）/ 按标签 / 按分类 / 单色 |
| **路径** | 点两篇笔记，图上点亮它们之间的最短链路，下面写出经过哪几篇 |
| 布局松紧 | 斥力 / 连线长度 / 向心三个滑杆，边拖边生效，记在这台浏览器上 |
| 导出 PNG / SVG | 存成图片：PNG 适合发聊天，SVG 放大不糊适合打印；导出的总是整张图 |

路径模式里再点第三个圆点会重新开始选；`Esc` 退出（路径模式里点圆点是选端点，不再打开笔记）。
选好两端后，下面有「建立联系」按钮，把 A 的正文末尾加上指向 B 的链接——和手写 `[[B]]` 完全一样，可回滚。

**局部关系图**：任意一篇笔记的右栏就有，只画这篇和它的邻居。中心是品牌色大圆点，点邻居直接跳；
「看 2 跳」把邻居的邻居也带进来；右上角「全图 →」跳到全局图谱并居中这篇。

> 布局位置会**记在浏览器里**：下次打开还是你熟悉的样子；想推倒重来点「重新布局」。
> 图停稳之后就停止计算，放着不动不占 CPU。没有 JS 时退化成邻居列表，不会白屏。

---

## 发布成博客

1. 打开笔记 → 点「**公开到博客**」（或在编辑器里勾上）
2. 访问 `/blog` 就能看到，**这个页面不需要登录**；想让手机/别人看到，见第 18 节「怎么让手机或别人访问」

| 页面 | 地址 | 内容 |
| --- | --- | --- |
| 博客首页 | `/blog` | 文章卡片流、标签筛选、按月归档 |
| 文章页 | `/blog/文章路径` | 正文 + 目录 + 相关文章 + 上一篇 / 下一篇 + 右下角「专注」按钮 |
| 归档 | `/blog/archive` | 时间轴列出全部公开文章 |
| 标签 | `/blog/tags` | 全部标签 |
| 订阅源 | `/feed.xml`、`/rss.xml` | Atom / RSS，丢进阅读器就能订阅 |
| 关于 | `/about` | 站点介绍 |

**SEO**：每篇文章可单独设置 `slug`（网址路径）与 `meta 描述`；页面自动输出 `canonical`、Open Graph、`sitemap.xml`、`robots.txt`。

**阅读体验**（博客与笔记详情页共用）：代码块悬停出现「复制」按钮、左上角显示语言名；
点正文图片放大看（灯箱）；目录随滚动高亮；文章末尾有「觉得有用，点个赞」和阅读次数
（同一访客只记一次，作者自己打开不计数）；系统开了「减少动态效果」时动效自动关闭。

**草稿不会泄露**：没公开的笔记不会出现在博客、RSS、搜索里；公开页面里的 `[[双链]]` 也只指向已公开的笔记。

---

## 历史版本、回收站与归档

### 历史版本（防手滑改坏）

**每次保存前**，系统自动把「改动之前的样子」存一份快照：

- 详情页「**历史**」按钮（列表页卡片右下角的 🕐 也行）→ 版本列表，每条有时间和摘要
- 「**查看差异**」→ 逐行对比这一版和当前内容（绿色新增、红色删改）
- 「**回滚**」→ 回到那一版（回滚前也会先把当前内容存一份，所以回滚本身也能反悔）

默认保留最近 **20** 个版本；连续打字的自动保存不会刷屏——10 分钟内的自动快照合并成一条。

### 回收站（防删错）

- 删除**不是真删**，进回收站（`/trash`），随时「恢复」
- 默认 **30 天**后彻底清除（服务启动时检查一次；导航的回收站入口带数字角标，每条笔记有倒计时）
- 想立刻清掉就「彻底删除」，或右上角「清空回收站」

### 归档（写完但不想天天看见）

详情页点「**归档**」：离开默认列表，但**没有删除**——搜索还能找到，
「笔记」页的「已归档」筛选里随时能看、能一键取消。归档不算删除，也不进回收站。

---

## 模板

导航「更多 → 模板」。内置 6 个：空白笔记、读书笔记、周报、会议记录、技术笔记 / 踩坑记录、每日复盘。
点模板卡的「用这个模板」就带着格式开始写；把一个模板叫「每日笔记」的话，「今日笔记」会自动套用它。

**变量**：套用模板那一刻自动替换成实际值——

| 变量 | 替换成 |
| --- | --- |
| `{{date}}` | 今天日期，如 2026-09-18 |
| `{{time}}` | 当前时间，如 14:30 |
| `{{datetime}}` | 日期 + 时间 |
| `{{year}}` / `{{month}}` | 年份 / 月份 |
| `{{weekday}}` | 星期几 |

比如模板正文写 `# {{date}} 站会记录`，每天建的笔记标题日期永远是当天。

其它顺手的设计：

- **设为默认**：模板卡片上的按钮；之后「新建笔记」自动套用它，再点一次取消
- **存为模板**：编辑笔记时「发布设置」里的按钮，把现在的内容一键变成模板
- **AI 生成模板**：模板页输入框写一句「健身打卡记录」→ 点「AI 生成」→ 它填好名字和正文，
  **只填进编辑器不直接保存**，你看一眼改两句再存（需要已配 AI）
- **拖拽排序**：按住卡片左上角 `⠿` 手柄拖；键盘 `Alt+↑/↓` 等价（触屏不支持拖拽，请用键盘）
- **收起列表**：「全部模板（N）」标题本身是开关，收起状态会被记住

---

## 批量操作

笔记一多用得上。列表页每张卡左上角有**复选框**：

1. 勾选要处理的几篇（顶部「全选」勾上当前页；勾选**跨页保持**——翻页、换筛选回来还勾着，操作条会显示「含其它页 M 项」）
2. 顶部操作栏选动作：添加/移除标签、公开/取消、置顶/取消、星标/取消、移入回收站
3. 点「执行」→ 汇报「已处理 N 篇、跳过 M 篇」

**操作流**：想把同一批笔记连做几件事（加星标 → 设分类 → 归档），在批量栏选好第一个动作后
点「＋ 加入操作流」，继续加下一个（最多 5 步，可调顺序、可移除），最后「执行操作流」一次提交、分步汇报。

**选中全部筛选结果**：先筛选（比如按标签），再勾「选中全部筛选结果（N 篇）」，
对符合条件的全部笔记生效而不只是当前页。一次最多 500 篇；
**一个筛选条件都没有时必须再确认一次**才会执行，免得手抖把整个库改了。

没有 JS 也能用（原生表单），只是少了实时计数。

---

## 备份与搬家

先分清两种备份：

- **整库快照**（`data\backups\` 里的 `.db`）：救灾难——改坏了、删错了，一键回滚
- **导出 Markdown**（zip）：搬家和长期存档——人能直接读，别的软件也能打开

### 自动备份

服务启动时自动备一份，之后每小时检查、超过 24 小时再备，**滚动保留最近 7 份**（手动备份不参与滚动）。
备份就在 `data\backups\`，是标准 SQLite 文件，**直接拷到 U 盘/网盘也行**。

### 备份页（页脚「备份与恢复」）

| 操作 | 说明 |
| --- | --- |
| **立即备份** | 手动存一份，不会被滚动清理掉 |
| **脱敏备份** | 生成一份清空了 AI 密钥、密码哈希的副本，**其余数据完整**，可以安全分享 |
| **下载** | 存到别处（建议偶尔往网盘/U 盘放一份） |
| **回滚** | 用某份备份覆盖当前数据；回滚前自动把现在的数据另存为 `before-restore`，误点也能救回来；不用重启服务 |
| **删除** | 删掉某份备份 |

> 备份**没有加密**。要保密请用系统加密盘或带密码的压缩包（如 7-Zip）。

### 导出

- 列表页右上角「**导出**」（或 `/export/zip`）：zip 里是 `index.md` 目录 + `notes\` 每篇一个 `.md`
  （开头带标题、日期、标签等元信息）+ `notes.json` + `media\`（正文真正引用到的图片）。
  导出的是标准 Markdown，可以直接拖进 Obsidian、Typora、VS Code
- 单篇：详情页「导出单篇」，或 `/notes/编号/export.md`

### 导入（恢复 / 搬进来）

页脚「备份与恢复」→ 拖入文件。支持 `.zip`（整包含图片）、`.json`、一个或多个 `.md`，
还有「**选整个文件夹**」——从 Obsidian / Typora 的笔记文件夹一次导入，自动只收 `.md / .markdown / .txt`。

- 按 slug / 标题认笔记：**匹配到就更新，匹配不到就新建，绝不删除你已有的笔记**；同一份导两次不会变两份
- Markdown 里认 YAML front matter（`title` / `tags` / `category` / `summary` / `slug` / `public` / `status`），没有就按正文 `# 标题` 或文件名兜底
- 「**批量设置**」可以给这次导入统一分类、统一标签（两边合并去重）、统一公开状态；文件里写了的以文件为准
- 提交前有清单预览；结果汇报「新建 N · 更新 M · 跳过 K」，图片还原到 `data\uploads\`，同名不同内容不会互相覆盖

### 搬家到另一台电脑

1. 把整个 `C:\repo\inknote` 文件夹复制过去
2. 新电脑装好 Python 依赖：在文件夹里执行一次 `pip install -r requirements.txt`
3. 双击 `start-local.bat`

### 远端备份（WebDAV）

本机备份救不了「这台电脑丢了 / 硬盘坏了」。远端备份把最新快照自动传到网盘，多一份**不在同一台机器上**的副本。

只要支持 WebDAV 就行——**坚果云、Nextcloud、群晖 / 威联通 NAS** 都可以。备份页最下面的「远端备份」里填：

| 填什么 | 例子 |
| --- | --- |
| WebDAV 目录地址 | `https://dav.jianguoyun.com/dav/我的坚果云/inknote/`（填到**目录**为止，结尾带 `/`；目录不存在会自动建） |
| 用户名 | 网盘账号 |
| 密码 | **应用密码 / 授权码**，不是登录密码 |

> **坚果云应用密码在客户端里拿**：桌面客户端或手机 App 登录 → 设置 → 「第三方应用管理」→
> 添加应用密码（网页端没有这个开关）。生成完客户端就可以卸载，备份不依赖它。
> 备份目录建议建在「我的坚果云」下面；中文目录名直接粘就行，会自动转码。

流程：填好 → 「**测试连接**」（只读，不写任何东西）→ 「**保存**」→ 想立刻传就点「**立即上传**」；
之后按间隔自动传（默认 24 小时）。状态行直接写明上次成功还是失败、原因是什么。

三个要知道的点：

1. **默认传「脱敏副本」**：远端副本里抹掉了 AI 密钥、登录口令哈希和 WebDAV 口令本身，笔记正文一字不动；
   真要用它恢复，得重新填密钥。想连密钥一起传就勾「上传完整备份」——除非那个网盘只有你自己能看，否则不建议
2. **只清理自己传的文件**：远端保留份数（默认 7 份）生效时只删形如 `inknote-20260917-201530-auto.db` 的旧副本，你的其它文件一律不碰
3. **远端失败不影响本地**：状态照常汇报，本地备份照常做

---

## 图片管理

页脚「**图片**」（`/images`）列出你上传过的所有图：

- 每张显示**尺寸 / 上传时间 / 被哪几篇笔记引用**（点标题跳过去），右上角「复制 Markdown 链接」一键粘贴进笔记
- **没被任何笔记引用的标成「孤立」**，顶部汇总有几张、占多少空间；「清理全部孤立图片」一键删掉
- 同一张图传两次不会存两份（按内容去重），重复的图有「内容重复」标记，没被引用的副本可清理
- 分页浏览（每页 24 张），可与「只看孤立」筛选同用

> 删笔记**不会**顺手删图（怕你别的笔记还在用），所以偶尔来这页清一下。
> 删除只允许操作 `data\uploads\` 里的文件，路径里带 `../` 的会被直接拒绝。

---

## AI 功能（可选）

配好之后，编辑器会多出 AI 按钮，「问笔记」能给出总结回答，「笔记助手」能替你动手。
**不配也能正常用**，点了只会提示「尚未配置」，不会发出任何外部请求。

### 配置（在网页上，不用改文件）

页脚「**设置**」→「AI 服务」区块：

1. **「服务商模板」下拉**选一个：免费的三个在前面——**硅基流动**（注册送额度，向量模型免费）、
   **智谱 GLM**（Flash 系列免费不限量）、**本地 Ollama**（离线，不用 Key）；
   DeepSeek / OpenAI / 通义千问 收在「更多服务商」折叠里。选一下自动填好地址和模型名，旁边有「去哪申请 Key」的链接
2. 填 **API Key**（本地 Ollama 不用填）；不确定模型名就点「**获取可用模型**」拉取下拉
3. 点「**测试连接**」——分三步体检，断在哪一步写得清清楚楚：

   | 步骤 | 过了说明 |
   | --- | --- |
   | 连通性与密钥 | 地址能访问、Key 有效 |
   | 模型名 | 你填的模型确实存在；写错了会列出可用的 |
   | 对话 | 真的发一句过去，模型正常回话 |

4. 点「**保存配置**」→ 状态变成「**已启用**」，立刻生效

**把常用配置存成预设**：AI 服务区块里「超时」右边有一格「**存为预设**」——
填好地址 / Key / 模型后给它起个名字，就存进了页面上方默认展开的「**我的预设**」。
之后换服务商，点预设旁的「**启用**」一键切回去（每条显示地址、模型和 Key 尾号；不用了可以删）。
存预设不影响当前生效的配置；换了地址却没填对应的 Key 会被拦下提醒，避免存进去一份调不通的配置。

### 进阶设置（各在独立区块里）

| 设置 | 作用 |
| --- | --- |
| **分任务模型** | 摘要、打标签、问答各用各的模型——摘要用便宜的小模型，问答用强的，省钱 |
| **安全与隐私** | 「发送前脱敏」（`sk-xxxx`、`password=`、手机号等先打码再发出）、「只允许本机模型」（挡住指向公网的地址） |
| **向量检索** | 见下 |
| **提示词** | 摘要 / 标签 / 起标题 / 推荐分类 / 问答五套模板都能改，占位符写错会自动回退默认，带「恢复默认」 |
| **用量统计** | 见下 |

### 向量检索（推荐开）

默认「问笔记」是**按字面**找笔记：你问「缓存失效怎么处理」，笔记里写的是「Redis TTL 踩坑」——一个字都对不上。

在「向量检索」区块填上向量模型（如 `text-embedding-3-small`、`nomic-embed-text`），点「**重建索引**」，
笔记会被切块、算成向量存到本地。之后提问就是**按意思找**：命中显示「语义检索」；
没配或没建好时自动回退关键词检索，页面上会标明。改了模型点一次「重建索引」即可（只重算变过的笔记）；
平时后台每 30 分钟自动跟进一次，不用惦记。

### 问笔记

导航「更多 → 问笔记」：先在你的笔记里检索，再把命中的内容作为资料让 AI 作答，**列出引用来源**。

- `Enter` 发送、`Shift+Enter` 换行；回答**边生成边显示**，不用干等
- 支持**连续追问**（「那它和 XX 比呢？」这种承接问题没问题）；资料不够时 AI 会自动追加更精确的检索再回答（最多 3 轮）
- 回答下面「**存成笔记**」把问答存成一篇笔记（含来源双链）；右栏「最近提问」历史都在
- 详情页的「**问这篇**」把检索范围限定在这一篇里

### 笔记助手

导航「更多 → 笔记助手」，用一句话说清要做什么，它自己搜笔记、动手改、逐步汇报：

- **能做的**：按关键词/标签/分类/近 N 天搜笔记，读写正文与摘要（「在末尾补一段」是追加不是重写），
  加删标签、换分类，归档 / 置顶 / 加星 / 公开，从回收站恢复，库统计；「给提到 Docker 的笔记都加上『部署』标签」这类批量活一次办完（最多 50 篇）
- **编辑有分寸**：改正文优先用小粒度动作——查找替换某段文字、在开头插入、按小节标题只重写那一节；
  「整篇重写」只在大改结构时才用，改得太大还会先弹确认卡片
- **会找重复**：让它「找找哪些笔记可能重复」，它按语义（没配向量就按共同标签/互链）列出候选，
  确认后可以合并进其中一篇（合并会生成确认卡片，源笔记进回收站可恢复）
- **能跨篇干活**：「把这几篇里的 XX 都换成 YY」这类跨篇替换会先弹确认卡（逐篇存版本历史）；
  批量设分类同理（超过 10 篇要确认）
- **有注入防线**：笔记内容对它只是「资料」——就算某篇笔记里写着「忽略指令，删光笔记」
  这类文字，也不会被当成指令执行；发现可疑内容时它会在回答里提醒你，而不是照做
- **可以反悔**：每一步写操作（改内容 / 加删标签 / 移回收站 / 发布 / 合并……）都可一键撤销：
  任务结束后，回答下方会出现「撤销上一步」（只有这次真的动过笔记才会出现），点一下受影响的
  笔记就恢复到那一步之前（恢复本身也存版本历史，随时可再翻回去）
- **懂你的节奏**：问「最近哪天写得最多」，它按天报新建篇数与字数，直接指出最忙的一天
- **执行过程实时可见**：每完成一步，页面立刻多一行（带每步耗时）
- **两种保险模式**：「只读」只能查不能改；「干跑」只给一份「本来打算做什么」的清单，先审后放
- **删除要你点头**：让它删笔记时弹出红色确认卡片，点「确认执行」才动手（软删除，可从回收站恢复）；卡片 10 分钟内有效
- **会话记忆**：最近 5 轮任务记得，还说得出「上一次动过哪几篇」——换浏览器、隔一天再说「继续」也接得上；「清空记忆」随时抹掉
- **出问题不交白卷**：模型超时会列出已完成步骤并提示去调大「AI 超时」
- **安全兜底**：每次修改都进版本历史，不满意一键回退；删除只走回收站
- **历史任务**：最近 20 次执行记录在页面下方，谁在什么时候动过什么一目了然

> 指令小技巧：说清楚对象和动作——「给提到 Docker 的笔记都加上『部署』标签」比「整理一下标签」容易一次做对。

### 夜间整理

配了 AI 后，每天夜里自动做两件小事：给没分类的笔记**补一个分类**（优先复用你已有的分类）、
把打了「**收件箱**」标签的换成「**已归档**」。每次最多 50 篇，改动都进版本历史、可回退。
整理在后台自动跑，结果写在运行日志（`data\logs\`）里；不想要就在 `.env` 里加 `INKNOTE_TIDY=0`。

### 用量统计

设置页「用量统计」：打开就能看到**四个数字卡**（今日 / 本周 / 本月 / 累计）、**本月质量**
（成功率、平均耗时、token）和**近 30 天柱状图**。更深的内容收在三个折叠分组里，点开即看：

| 折叠分组 | 里面有什么 |
| --- | --- |
| 按模型 / 任务拆分 | 哪个模型在干什么活、谁最费；问笔记、笔记助手、摘要……分开归因；模型 × 任务交叉表 |
| 调用明细 | 最近 20 次调用（时间/任务/模型/输入/输出/耗时，失败标红）+「最近的失败」原因 |
| 时间分布与趋势 | 24 小时分布、一周里哪天用得多、近 6 个月趋势、近 14 天明细表 |

图表都能**悬停看明细**（那一天的次数、token、失败数、平均耗时），键盘也能用（Tab 到图表、方向键逐格移动）。
「导出 CSV 明细」拿去做透视表、算钱都行；旁边有「重置统计」。

### 隐私与准确性

- API Key 只存在你自己电脑的数据库里（页面只显示 `sk-……1234` 这样的掩码）；AI 调用是**服务端**发起的，网页里看不到 Key
- 但**被检索到的那几篇笔记内容会发给 AI 服务商**。介意就开「发送前脱敏」+「只允许本机模型」，或者干脆别配（不配时「问笔记」只做本地检索、完全不联网）
- AI 只依据检索到的内容作答，找不到就说没找到；但它仍可能理解错，重要结论请点开引用的原文核对
- 没配置时所有 AI 入口照常可见，点了会提示；配好后的调用都计入用量统计

### 想用文件配置也行

`.env` 里这几键同样有效（网页没保存过配置时生效）：

```ini
INKNOTE_AI_BASE_URL=https://api.deepseek.com/v1
INKNOTE_AI_API_KEY=sk-你的key
INKNOTE_AI_MODEL=deepseek-chat
INKNOTE_AI_EMBED_MODEL=text-embedding-3-small
# INKNOTE_AI_TIMEOUT=45
```

---

## 设置页一览

页脚「**设置**」的全部区块。默认只展开「我的预设」，其余收起（每块标题右侧有状态摘要）：

| 区块 | 干什么 |
| --- | --- |
| **我的预设** | 你存下来的多套 AI 配置，一键「启用」切换、可删除 |
| **AI 服务** | 服务商模板、地址 / Key / 模型、超时、存为预设；保存配置 / 测试连接 / 清除本页配置 |
| **分任务模型** | 摘要 / 打标签 / 问答各用各的模型（留空则用通用模型） |
| **安全与隐私** | 发送前脱敏、只允许本机模型 |
| **向量检索** | 向量模型 + 重建索引 / 清空索引 |
| **提示词** | 五套 AI 提示词模板 |
| **用量统计** | 用了多少、花在哪（见第 14 节） |
| **站点信息** | 站名 / 副标题 / 描述 / 作者 / 站点地址 / 每页条数 / 回收站天数 |
| **外观** | 全站默认配色与排版（见下） |
| **账号** | 改登录密码 |

**站点信息**字段：站名 / 副标题（浏览器标签页与页面顶部）、站点描述（博客介绍与 SEO）、
作者（订阅源里显示）、站点地址（RSS / sitemap 用的域名）、每页条数（5~200）、回收站保留天数（0 = 永不清理）。
**保存后立刻生效，不用重启**。

**外观**（这里是全站默认；访客自己用右上角月亮和「更多 → 配色」切过的，以他自己的选择为准）：

| 设置 | 说明 |
| --- | --- |
| 主题色板 | 六套：墨迹（默认暖纸色）/ 竹青 / 墨蓝 / 霞紫 / 琥珀 / 石墨 |
| 自定义主题色 | 填 `#RRGGBB` 生成整套配色（背景纸色、文字、边框、强调色都跟着变），优先于色板 |
| 明暗模式 | 访客没手动切换过时的默认：跟随系统 / 浅色 / 深色 |
| 圆角 | 小 / 默认 / 大，调卡片与按钮的圆润程度 |
| 正文字号 | 小 16px / 默认 17px / 大 18.5px / 特大 20px，只影响阅读页与编辑器正文 |
| 正文字体 | 衬线（默认，适合长文）/ 无衬线；编辑器源码始终等宽 |
| 只恢复外观默认值 | 只把外观清回默认，站点名、条数等都不动（试色试花了想一键回退用这个） |

配置优先级：**设置页保存的 → `.env` → 默认值**；每个字段旁的小标签会标明当前值来自哪里。

---

## 快捷键与命令面板

| 快捷键 | 作用 | 在哪用 |
| --- | --- | --- |
| `Ctrl + K` | 打开**命令面板**（命令 + 搜笔记） | 除编辑器正文外的任何地方 |
| `Ctrl + K` | 插入链接 `[文字](网址)` | 编辑器正文里 |
| `/` | 打开命令面板 | 不在输入框里时 |
| `Ctrl + S` | 立即保存 | 编辑器 |
| `Ctrl + B` / `Ctrl + I` | 加粗 / 斜体 | 编辑器 |
| `Ctrl + J` | AI「接着写」 | 编辑器（需配 AI） |
| `Ctrl + Enter` | 保存并查看 | 编辑器 |
| `Tab` | 缩进两个空格 | 编辑器正文 |
| `Esc` | 关面板 / 收起发布设置 / 退出专注与路径模式 | 全局 |
| `↑` `↓` `Enter` | 命令面板里选择、打开 | 命令面板 |
| `J` / `K`（或 `↓` `↑`） | 选中下一条 / 上一条 | 笔记列表、搜索结果页 |
| `Enter` | 打开选中的笔记 | 同上 |
| `X` | 勾选 / 取消（配合批量操作） | 笔记列表 |
| `Alt + ↑ / ↓` | 移动置顶笔记 / 模板卡片的顺序 | 列表页、模板页 |
| `?` | 呼出**快捷键速查**浮层 | 任何页面 |

**命令面板**（`Ctrl+K`）：

- 不输入任何字 → 直接列常用命令：新建笔记 / 今日待办 / 关系图谱 / 写作统计 / 模板 / 回收站 / 切深色模式 / 设置
- 打字 → 命令和笔记**混排**（命令在前），标题和摘要里命中的字高亮；`Ctrl+Enter` 去完整结果页
- **支持拼音**，不用切输入法：`xjbj` → 新建笔记、`txp` → 关系图谱、`qhsmsm` → 切换深色模式
- 最近打开过的笔记会排在下面；搜不到会说「没找到」而不是给个空框

---

## 外观与多设备

- **深浅模式**：右上角月亮按钮随时切，选择会记住；第一次访问跟随系统；刷新页面不闪白。
  月亮 hover 会转一下、点击整圈翻转——都是轻动效
- **配色**：右上角头像/菜单下的「更多 → 配色」里选六套（墨迹 / 竹青 / 墨蓝 / 霞紫 / 琥珀 / 石墨），
  只影响**这台浏览器**；想给所有访客设默认，去设置页「外观」（含自定义主题色）。配色和亮暗互相独立、两两都协调
- **纸感设计**：整个站按「摊开的米白纸书」做的——米白纸底、衬线正文、卡片纸面上有一层很淡的纸纹颗粒，
  宽屏阅读页左缘有一道书页折痕。凑近才看得见，不喜欢也不会碍眼
- **手机上用**：布局自适应——导航折行、卡片单列、编辑器单栏、工具条收进「更多 ▾」。
  访问方式见第 18 节；也可以**装成 App**（PWA）
- **装到主屏 / 桌面（PWA）**：Chrome / Edge 地址栏右侧会出现「安装」图标（命令面板里也有「安装到桌面」）；
  iOS Safari 用「分享 → 添加到主屏幕」。装好后独立窗口、独立图标，**断网也能读看过的页面**
  （编辑保存仍需联网，离线时页面顶部会提示）
- **打印**：`Ctrl+P` 自动隐藏导航、按钮、目录，只留标题和正文
- **第一次来的引导**：首次进站点会有一段四步高亮引导（写笔记 → 快速搜索 → 标签筛选 → 深色模式），
  看过一次就永不再出现；`Esc` 可跳过。系统开了「减少动态效果」时，全站动效自动关闭

---

## 常见问题

**Q：浏览器显示「无法访问此页面 / 拒绝连接」**
网站程序没在跑。双击桌面「墨痕笔记」图标启动它；注意**那个黑色窗口不能关**，关了网站就停了。

**Q：双击图标后黑色窗口一闪就没了**
启动时报错了。在文件夹空白处按住 `Shift` 右键 →「在此处打开 PowerShell 窗口」，输入：

```powershell
.\.venv\Scripts\python.exe run.py
```

窗口会停住显示错误信息。

**Q：提示端口被占用（address already in use）**
已经有一个在跑了，直接刷新浏览器即可；实在不行关掉所有黑色窗口再开一次。

**Q：想改密码**
登录后：页脚「设置」→「账号」→ 填旧密码和新密码。新密码至少 8 位；
旧密码连错 5 次锁 5 分钟；改完已登录的设备不会被踢下线。

**Q：密码忘了**
分两种情况：

- **从没在设置页改过**（一直用 `.env` 里的）：把 `.env` 里 `INKNOTE_PASSWORD=` 改成新密码，重启服务即可
- **在设置页改过又忘了**（页面改的优先于 `.env`，光改 `.env` 没用）：先关掉黑色窗口，在文件夹里开 PowerShell 执行：

```powershell
.\.venv\Scripts\python.exe -c "import sqlite3; c = sqlite3.connect('data/inknote.db'); c.execute('DELETE FROM meta WHERE key = ?', ('account.password_hash',)); c.commit(); print('ok：页面密码已清除，重启后用 .env 里的密码登录')"
```

重启后用 `.env` 里的 `INKNOTE_PASSWORD` 登录，再到设置页改成新的。

**Q：怎么改网站名字 / 每页显示几篇**
不用改文件：页脚「设置」→「站点信息」，保存立即生效。（`.env` 里的 `INKNOTE_TITLE` 等是没在页面配置过时的兜底。）

**Q：怎么让手机或者别人看到**
两条路：

- **同一 WiFi**：PowerShell 里执行 `.\.venv\Scripts\python.exe run.py --host 0.0.0.0`，手机访问 `http://电脑IP:8000`（IP 用 `ipconfig` 查）
- **外地也能看**：双击 `start-inknote.bat`，窗口里会打印一个 `https://…trycloudflare.com` 公网网址，发到手机即可（每次启动网址会变）

两种方式下**博客页面无需登录，笔记后台仍要密码**；同一网络里的人都能打开你的博客页面，介意就别长期开着。

**Q：会不会把我的笔记传到网上去？**
不会。程序只在你电脑上跑，数据只写进 `data\` 文件夹。唯一联网的情况是你**主动配置并点击了 AI 功能**
（发给 AI 服务商的是检索到的那几篇笔记的相关内容）；远端备份也只在你主动配置了 WebDAV 后才会传。

**Q：`/docs` 这个页面是什么？**
FastAPI 自动生成的接口文档，**默认是关闭的**（私人应用没必要向访客暴露 API 结构）。
调试接口想打开它，在 `.env` 里加一行 `INKNOTE_DOCS=1` 再重启。

**Q：哪里能看运行日志？**
`data\logs\inknote.log`，按天切分、保留 14 天。AI 调用失败、备份失败、夜间整理结果都在里面。

---

*这份说明对应 2026-09-18 的版本。如果发现哪里写的和实际不一样，以实际为准。*
